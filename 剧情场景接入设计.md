# Thiết kế kết nối cảnh cốt truyện

## Nguyên tắc cốt lõi

Cảnh cốt truyện không phải một nút chức năng frontend, mà là trạng thái biểu diễn ẩn đằng sau thao tác gameplay.

Người chơi nhìn thấy là thao tác tự nhiên. Script dựa vào thao tác tạo `StorySceneSession`, trả về effect `enter_story_scene` và `request_story_scene_context`, do tầng thích ứng frontend chuyển sang biểu diễn chính văn cốt truyện.

Frontend không nên hiển thị tên tầng cốt truyện nội bộ như một cổng vào độc lập.

## Cấu trúc nội bộ

Kiểu dữ liệu cốt lõi ở:

```text
src/汤泉/员工系统/types.ts
```

Trường chính:

- `StorySceneSession`: ghi lại cảnh cốt truyện hiện tại.
- `StorySceneContextRequest`: cấu trúc hóa context giao cho tầng tiếp nhận chính văn Tavern.
- `storySceneSessionId`: mã số cảnh cốt truyện gắn với đơn hàng hoặc ghi chép dịch vụ khách.
- `enter_story_scene`: thông báo frontend chuyển vào tầng biểu diễn cốt truyện.
- `request_story_scene_context`: yêu cầu giao context cấu trúc hóa cho tầng tiếp nhận chính văn.
- `exit_story_scene`: thông báo frontend thoát tầng biểu diễn cốt truyện.

Các tên này chỉ thuộc tầng kỹ thuật, không yêu cầu người chơi nhìn thấy.

## Kết nối gameplay chủ tiệm

Gameplay chủ tiệm vẫn lấy kinh doanh làm chính, cốt truyện chỉ nhúng tự nhiên vào quản lý nhân viên và hiện trường dịch vụ.

### Hồ sơ nhân viên

Thao tác frontend:

- Trò chuyện
- Xem trạng thái
- Trao đổi riêng
- Tương tác thân mật
- Hẹn hò ra ngoài

Cổng vào script:

```ts
openEmployeeStoryScene()
```

Ánh xạ điển hình:

- `Trò chuyện` -> `mode: '员工谈话'` (đối thoại nhân viên)
- `Hẹn hò ra ngoài` -> `mode: '外出约会'` (hẹn hò ra ngoài)
- `Tương tác thân mật` hoặc `trải nghiệm riêng tư` -> `mode: '私人体验'` (trải nghiệm riêng tư)

Biểu hiện frontend:

- Bấm thao tác tự nhiên ở trang thông tin nhân viên.
- Ẩn việc tạo cảnh cốt truyện.
- Chuyển sang hộp thoại galgame hoặc tầng chính văn cốt truyện.

### Đơn hàng dịch vụ

Thao tác frontend:

- Xem hiện trường
- Kiểm tra dịch vụ
- Chủ tiệm can thiệp
- Tiếp tục dịch vụ
- Kết thúc hiện trường

Cổng vào script:

```ts
openServiceStoryScene()
```

Ánh xạ điển hình:

- `Xem hiện trường` -> `mode: '查看工作'` (xem công việc)
- `Kiểm tra dịch vụ` -> `mode: '服务检查'` (kiểm tra dịch vụ)
- `Chủ tiệm can thiệp` -> `mode: '老板介入'` (chủ tiệm can thiệp)

Biểu hiện frontend:

- Đơn hàng vẫn ở trong quy trình kinh doanh.
- Chỉ khi bấm xem hoặc can thiệp mới tạm chuyển sang tầng biểu diễn cốt truyện.
- Sau khi thoát quay về trang đơn hàng/kinh doanh.

## Kết nối gameplay khách

Gameplay khách lấy trải nghiệm dịch vụ và chính văn cốt truyện làm chính, việc bấm chọn chỉ dùng để chọn trước khi nhập vai và quyết toán sau khi kết thúc.

### Trải nghiệm dịch vụ

Thao tác frontend:

- Chọn dự án
- Chỉ định nhân viên
- Xem trước giá
- Bắt đầu dịch vụ
- Kết thúc dịch vụ

Cổng vào script:

```ts
startCustomerServiceScene()
finishCustomerService()
```

Quy trình:

```text
Bắt đầu dịch vụ
-> Tạo hồ sơ khách của người chơi
-> Tạo đơn hàng
-> Tính giá
-> Tạo cảnh cốt truyện
-> Frontend chuyển sang tầng biểu diễn chính văn cốt truyện
-> Sau khi dịch vụ kết thúc gọi quyết toán
```

### Liên hệ và hẹn riêng

Thao tác frontend:

- Yêu cầu liên hệ
- Hẹn riêng

Cổng vào script:

```ts
requestEmployeeContact()
startPrivateDateScene()
```

Quy trình:

```text
Yêu cầu liên hệ
-> Script phán định có đồng ý hay không theo số liệu
-> Sau khi đồng ý xuất hiện thao tác hẹn riêng
-> Hẹn riêng tạo cảnh cốt truyện mới
```

## Kết nối gameplay nhân viên

Gameplay nhân viên lấy trải nghiệm ngày làm việc làm chính, frontend chịu trách nhiệm ca trực, tiếp khách, chỉ định, đồng nghiệp, thu nhập và cổng vào quyết toán; chính văn cốt truyện do thao tác làm việc tự nhiên kích hoạt.

### Ca trực và tiếp khách

Thao tác frontend:

- Đi làm
- Đến ca chờ
- Tiếp khách
- Kết thúc dịch vụ
- Nghỉ ngơi
- Quyết toán tan ca

Cổng vào script:

```ts
startWaiterWork()
markWaiterOnDuty()
generateWaiterCustomer()
startWaiterServiceScene()
finishWaiterService()
settleWaiterDay()
```

Quy trình:

```text
Đi làm
-> Đọc/đăng ký xếp ca hôm nay
-> Đến ca chờ
-> Script sinh dữ liệu cơ bản của khách
-> Thao tác tiếp khách tạo cảnh cốt truyện
-> Chính văn tiếp nhận quá trình dịch vụ
-> Sau khi dịch vụ kết thúc ghi đánh giá, tiền boa, thu nhập thêm
-> Quyết toán tan ca lương ngày và thu nhập cá nhân
```

### Chỉ định, chủ tiệm và đồng nghiệp

Thao tác frontend:

- Vào đồng hành chỉ định
- Chủ tiệm tuần tra
- Chủ tiệm trò chuyện
- Đến phòng nghỉ
- Hoạt động sau giờ làm

Cổng vào script:

```ts
applyWaiterNomination()
startBossInspectionScene()
startBossTalkScene()
startCoworkerScene()
startAfterWorkScene()
```

Ánh xạ điển hình:

- `Tiếp khách` -> `mode: '接客服务'` (dịch vụ tiếp khách)
- `Vào đồng hành chỉ định` -> `mode: '被指名陪伴'` (được chỉ định đồng hành)
- `Chủ tiệm tuần tra` -> `mode: '老板巡查'` (chủ tiệm tuần tra)
- `Chủ tiệm trò chuyện` -> `mode: '老板谈话'` (chủ tiệm trò chuyện)
- `Đến phòng nghỉ` -> `mode: '同事互动'` (tương tác đồng nghiệp)
- `Hoạt động sau giờ làm` -> `mode: '下班休息'` (nghỉ ngơi sau tan ca)

## Quy tắc thích ứng frontend

Frontend chỉ cần tiêu thụ 3 loại effect:

```ts
enter_story_scene
request_story_scene_context
exit_story_scene
```

Đề xuất thích ứng:

- Nhận `enter_story_scene`: chuyển sang view biểu diễn cốt truyện ẩn.
- Nhận `request_story_scene_context`: giao context cho tầng tiếp nhận chính văn Tavern.
- Nhận `exit_story_scene`: quay về trang nguồn.

Văn bản UI dùng từ ngữ thao tác:

- Bắt đầu dịch vụ
- Trò chuyện
- Xem hiện trường
- Can thiệp
- Tiếp tục
- Kết thúc
- Hẹn riêng
- Đến ca chờ
- Tiếp khách
- Chủ tiệm tuần tra
- Đến phòng nghỉ
- Quyết toán tan ca

Không được làm tầng cốt truyện nội bộ thành nút, menu hay trang chức năng độc lập.

## Chưa làm hiện tại

- Chưa kết nối tầng tiếp nhận chính văn Tavern thật.
- Chưa quyết định văn bản UI chính thức tương ứng với mỗi thao tác.
- Chưa triển khai adapter bật/tắt thật cho entry world book và quy tắc biến.
- Chưa kết nối đồng bộ cảnh hiện tại của MVU `stat_data`.
