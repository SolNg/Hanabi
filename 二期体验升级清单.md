# Danh sách nâng cấp trải nghiệm giai đoạn 2 của Hoa Chưa Nở

> File này là cổng TODO, ghi lỗi (BUG) và tiếp nối ngữ cảnh duy nhất có hiệu lực trong giai đoạn nâng cấp trải nghiệm giai đoạn 2 (P5) và mở rộng hệ thống kinh doanh.
> `项目进度清单.md` (Danh sách tiến độ dự án) và `维护升级清单.md` (Danh sách nâng cấp bảo trì) chỉ dùng làm lịch sử đã hoàn thành và baseline chống hồi quy; không xóa, không viết lại, không chạy lại các task cũ.

## Giai đoạn hiện tại

- Giai đoạn: P5-1～P5-7, P6 (tương thích cơ bản màn hình ngang điện thoại), P8 (sửa lỗi khởi động), P9 (audit máy thật), P10 (sắp xếp lại độc lập), P11/P14 (phát hành chính thức) và P12/P13 (sửa lỗi + nghiệm thu Chrome ngoài) đều đã hoàn thành.
- Ưu tiên hiện tại: nút "Quay lại" trong menu chủ tiệm ở P12 đã được nâng lên cao 48px và pass phúc tra 3 mốc Chrome ngoài; P13 đã pass toàn bộ chuỗi nghiệm thu, `tq-v1.0.5` đã phát hành và không còn vướng mắc tồn đọng của vòng này.
- Cổng vào vận hành chính thức: `src/汤泉/脚本/零层美化/`; không sửa hoặc dùng HTML preview làm đối tượng nghiệm thu.
- Bundle tiếng Trung đã phát hành hiện tại: `tq-v1.0.5` tại `dist/汤泉/脚本/零层美化/index.js`, 9,153,431 bytes, SHA256 `890F4496C0D05D6ABC7517C15A4888586F18C84B2959A34B27A5A12EFC85123E`.
- Bundle tiếng Trung cục bộ hiện tại: `dist/汤泉/脚本/零层美化/index.js` giống hệt từng byte với bản trên remote/CDN của `tq-v1.0.5`, `9,153,431 bytes`, SHA256 `890F4496C0D05D6ABC7517C15A4888586F18C84B2959A34B27A5A12EFC85123E`.
- Baseline rời máy thật ngoài của P6: thời gian cập nhật, số byte và checksum của 2 save `slot-01`, `slot-02` gốc của người dùng đều không thay đổi; không còn tồn đọng save test, entry world book test, object QA toàn cục, iframe tạm, style toàn màn hình/z-index chồng lấp hay giả lập thiết bị; service 8879 đã dừng.
- Kết quả phát hành: commit `604601ab2424c3cb9866a966da7caa084fa6f643` đã push lên `origin/main`, tag chú thích `tq-v1.0.5` đã phát hành; cả GitHub raw và CDN theo tag cố định đều trả HTTP 200, số byte/SHA256 khớp từng byte với bản cục bộ.
- Ranh giới phạm vi làm việc: repo đã có rất nhiều thay đổi, xóa và file chưa track không liên quan tới task này; chỉ động vào các file P5/P6 đã ghi rõ trong danh sách này, không dọn dẹp hay ghi đè thay đổi của người dùng.

## Ghi chú công bố phiên bản

- 2026-07-16: thay 21 tấm ảnh minh họa của Tiểu Sách (4 tấm), Vong Khước (1 tấm), Tinh Trần (2 tấm), Hành Tây (3 tấm), Thước Hàn (3 tấm), Chúc Tịch (3 tấm), Thần Phụ (2 tấm), Ngư Băng (3 tấm) bằng phiên bản đã tách nền trong suốt lại; chỉ cập nhật ánh xạ ảnh minh họa gốc trong `ui/app/mediaCatalog.ts`, ảnh đại diện giữ nguyên. Đã vào bản build tiếng Trung cục bộ kế tiếp, chưa phát hành CDN; bản công bố phiên bản sau cần ghi thêm mục "sửa nền ảnh minh họa chưa được tách".
- 2026-07-16: thêm giao diện màn hình ngang điện thoại độc lập, sắp xếp lại trang tiêu đề, panel save/setting/mode, HUD 3 chế độ chơi, menu, AIRP và popup; hỗ trợ fullscreen trình duyệt chuẩn và chế độ giảm cấp immersive trong viewport nội dung trình duyệt, thích ứng chiều cao thanh công cụ động và vùng an toàn (safe area). Viewport CSS chính xác của Chrome đã bao phủ các mốc kiểm tra xoay ngang 740×320, 844×390, 932×430 và xoay dọc 390×844; không được quảng cáo là fullscreen vật lý thiết bị hay đã nghiệm thu trên iPhone/Safari thật.
- 2026-07-17: P12 sửa lỗi mục đầu tiên trong menu chủ tiệm bị cắt khi ở màn hình ngang điện thoại gập/siêu rộng thấp, giảm mật độ HUD 3 chế độ chơi, và nâng vùng chạm nút "Quay lại" trong menu chủ tiệm lên cao 48px; đã pass 3 mốc Chrome ngoài, đi kèm phát hành `tq-v1.0.5`.
- 2026-07-17: P13 sửa lỗi phần chính văn AIRP hiển thị không có tag `<content>` bị hiểu nhầm là rỗng khi mở lại, load save hoặc chuyển Galgame; Chrome ngoài đã hoàn thành nghiệm thu 5 trang thoại atri, chính văn/Galgame, mở lại, lưu/tải save, rehydrate lặp lại và chuỗi phân giải dùng chung của 3 chế độ chơi, đi kèm phát hành `tq-v1.0.5`.
- Thông báo cập nhật `tq-v1.0.5`: sửa mục đầu tiên trong menu chủ tiệm ở màn hình ngang màn hình gập/siêu rộng thấp bị cắt và vùng chạm nút quay lại quá nhỏ, tối ưu mật độ thông tin màn hình ngang cho chủ tiệm/khách/nhân viên; sửa một số phản hồi AIRP không có tag `<content>` bị mất lời thoại khi mở lại, load save hoặc chuyển Galgame.

## Task mới thêm vòng này: P6 giao diện độc lập màn hình ngang điện thoại và thích ứng fullscreen trong trình duyệt

### Yêu cầu gốc của người dùng

1. Thích ứng điện thoại, nhưng không phải bản dọc, mà phải làm thích ứng bản ngang.
2. Vấn đề cốt lõi: cái gọi là fullscreen hiện tại không phải fullscreen điện thoại thật, mà thường chỉ là "fullscreen trong trình duyệt".
3. Cài đặt môi trường kiểu giả lập điện thoại, hoặc dùng Google Chrome có thể kiểm soát chính xác đến "độ phân giải fullscreen bên trong trình duyệt điện thoại".
4. Thích ứng điện thoại không phải chỉ scale responsive đơn giản, mà phải dựa trên tư liệu hiện có làm riêng một bộ giao diện được sắp xếp lại, thiết kế lại.
5. Ý tưởng thiết kế bắt buộc phải tham khảo các App điện thoại thành công.

### Hiện tượng vấn đề và mục tiêu cập nhật

- UI chính thức hiện tại chủ yếu dùng canvas desktop 16:9 và Fullscreen API của trình duyệt, chưa được chứng minh là có thể chạy ổn định dưới viewport nội dung, safe area, thu/mở thanh địa chỉ/toolbar và tương tác chạm của trình duyệt màn hình ngang điện thoại thật.
- Mục tiêu vòng này là thêm một bộ layout điện thoại màn hình ngang độc lập mà không phá vỡ bản desktop; tái sử dụng background, ảnh minh họa, khung tên, hộp thoại, menu và ngôn ngữ hình ảnh hiện có, nhưng tổ chức lại tầng thông tin, vùng nóng chạm, đường dẫn panel và kích thước popup.
- "Fullscreen trong trình duyệt điện thoại" lấy viewport CSS thực tế khả dụng của trình duyệt làm nguồn sự thật, không giả định bằng độ phân giải vật lý thiết bị; phải bao phủ cả chiều cao viewport động, safe area và hai loại hành vi Fullscreen API khả dụng/không khả dụng.

### Phạm vi ảnh hưởng và ranh giới triển khai ban đầu

- Cổng vào chính thức: `src/汤泉/脚本/零层美化/`; không sửa hoặc phụ thuộc HTML preview.
- Dự kiến ảnh hưởng: `ui/app/App.vue`, kết nối iframe/fullscreen game chính thức, component layout và style, nhận diện thiết bị màn hình ngang, menu/popup/Loading/lớp phủ hướng dẫn, công cụ tự động hóa và test viewport di động; file chính xác sẽ bổ sung sau khi audit mã nguồn.
- Bản desktop tiếp tục giữ layout và ngữ nghĩa chức năng hiện có; bản điện thoại dùng nhánh layout độc lập hoặc tầng component riêng, không dùng scale CSS toàn trang để thay thế thiết kế.
- Không chủ động thay đổi cấu trúc save, MVU schema, chính văn world book, prompt AI, queue AI duy nhất hay công thức kinh tế; nhưng phải hồi quy đủ 3 chế độ chơi, lưu/tải save, MVU, world book động, AIRP, tự động lưu và quyết toán kinh tế.
- Không khôi phục hoặc thêm mã nguồn/build entry/từ điển/công cụ test tiếng Việt.

### Tiêu chí nghiệm thu ban đầu

- [x] Đã ghi nhận yêu cầu gốc, mục tiêu, phạm vi, rủi ro và cổng nghiệm thu của người dùng.
- [x] Audit chuỗi gọi thực tế desktop/fullscreen/iframe/chạm/hướng dẫn/popup/3 chế độ chơi, ghi lại nguyên nhân gốc và component có thể tái sử dụng.
- [x] Nghiên cứu và ghi lại các pattern thiết kế UI App/game màn hình ngang di động thành công có thể kiểm chứng, chỉ hấp thụ kiến trúc thông tin và nguyên tắc tương tác, không copy tư liệu thương hiệu.
- [x] Xây dựng môi trường test màn hình ngang trình duyệt điện thoại chính xác có thể lặp lại; ghi lại DPR, CSS viewport, viewport động, safe area và ranh giới chiếm dụng UI trình duyệt.
- [x] Bản điện thoại dùng layout độc lập, không xuất hiện scale toàn thể canvas desktop, xếp chồng dọc, cuộn trang ngoài ý muốn hay bị thanh trình duyệt che khuất.
- [x] Bao phủ 8 nhóm fixture phân loại màn hình ngang/chiều cao thấp, và test thực tế trên Chrome ngoài ở các mốc 740×320, 844×390, 932×430 (ngang) và 390×844 (xoay dọc).
- [x] Trang tiêu đề, chọn chế độ, cổng tạo save mới, 12 slot save, setting, màn hình chính/menu của chủ tiệm/khách/nhân viên, AIRP, danh bạ, chọn thời gian, layout xác nhận/Loading/hướng dẫn và điều khiển fullscreen góc trên phải đều đã hoàn thành cổng nghiệm thu bằng code hoặc máy thật.
- [x] Đã xác minh vùng chạm 44/48px, cỡ chữ động, độ tương phản, safe area, vùng nhập/cuộn và đường quay lại panel con; ở chiều cao thấp 740×320, hàng save cuối cùng có thể cuộn để nhìn thấy hoàn toàn và vùng thao tác vẫn cố định.
- [x] Vào/thoát Fullscreen API gốc đã pass; khi bị từ chối cưỡng bức thì chế độ giảm cấp trong viewport nội dung trình duyệt cũng pass, sau khi thoát thì iframe, z-index cha, overflow, node cha gốc và cùng một `contentWindow` đều được khôi phục hoàn chỉnh.
- [x] Namespace style desktop được cách ly; tự động hóa cốt lõi của 3 chế độ chơi, lưu/tải save, MVU, world book, AIRP, queue AI đơn, tự động lưu và quyết toán kinh tế không bị hồi quy.
- [x] Đã hoàn thành ESLint có định hướng, các `scripts/test_tangquan_*.cjs` cốt lõi và của P6, Prettier, `pnpm build:dev`, số byte/SHA256 bundle và kiểm tra nội dung tạm thời.
- [ ] `pnpm exec tsc --noEmit` cấp toàn repo vẫn fail do nợ kiểu dữ liệu (type debt) toàn repo có sẵn: 307 dòng output liên quan đến khai báo toàn cục `TypeFest`, các kiểu không nhất quán cũ của Trí Não/NAI/WTC và Hoa Chưa Nở; `gameUi.ts`, `mobileViewport.ts` được sửa trong vòng này không xuất hiện trong lỗi, biên dịch TypeScript webpack pass. Vòng này không vượt phạm vi sửa các vấn đề lịch sử này.
- [x] Sau khi được người dùng đồng ý, chỉ dùng Chrome ngoài đã mở sẵn của người dùng tại `http://127.0.0.1:8000/` để hoàn thành nghiệm thu máy thật chính thức; trạng thái test, inject, object toàn cục, giả lập thiết bị và service 8879 đều đã dọn sạch.
- [x] Đã hoàn thành đối chiếu nguyên nhân gốc, chuỗi gọi thực tế, căn cứ thiết kế, file đã sửa, kết quả test, kết quả Chrome ngoài, kết quả dọn dẹp, vấn đề tồn đọng và trạng thái phát hành.

### Tính tương thích, nghiệm thu ngoài và đánh dấu phát hành

- Save/save cũ: dự kiến không thêm trường persist mới; nếu bắt buộc phải thêm sau khi audit, chỉ được cung cấp migration mặc định và không được thay đổi trạng thái chơi của save cũ.
- MVU/world book/AI/kinh tế: dự kiến không thay đổi nguồn sự thật và hợp đồng dữ liệu; sau khi triển khai phải hoàn thành xác minh chống hồi quy.
- 3 chế độ chơi: chủ tiệm, khách, nhân viên đều bị ảnh hưởng bởi layout, chức năng phải đạt được tương đương.
- Chrome ngoài: cần thiết; trước khi kết nối chính thức phải gửi yêu cầu kết nối debug cho người dùng, do người dùng bấm đồng ý.
- Thông báo phiên bản tiếp theo: cần thiết; ít nhất phải ghi "thêm giao diện độc lập màn hình ngang điện thoại" và "sửa thích ứng fullscreen/viewport khả dụng trong trình duyệt trên điện thoại", kết quả cuối cùng lấy test thực tế làm chuẩn.
- Trạng thái hiện tại: P6 đã triển khai và pass tự động hóa cục bộ, build dev chính thức và nghiệm thu viewport chính xác trên Chrome ngoài; chưa commit, chưa push, chưa gắn tag, chưa phát hành CDN.

### P6 audit ban đầu, nguyên nhân gốc và chuỗi gọi thực tế

- `gameUi.ts::syncInlineFrameSize()` chỉ dựa vào chiều rộng host `<=760` để phán đoán "màn hình dọc", sau đó ép iframe thành `9:16`; khi rộng hơn 760 lại cố định thành `16:9`. Nó không đọc `visualViewport`, chiều cao toolbar động, khả năng chạm hay hướng của trình duyệt điện thoại, nên điện thoại màn hình ngang nhỏ sẽ bị xếp nhầm thành bản dọc, còn điện thoại màn hình ngang rộng hơn sẽ nhận iframe 16:9 cố định vượt quá chiều cao khả dụng thực tế của trình duyệt.
- `App.vue`, `CustomerPlay.vue`, `WaiterPlay.vue` đều dùng media query `max-width` để thu nhỏ/đơn cột hóa các phần tử desktop; `App.vue` ở `max-width:760px` lại ép canvas gốc thành `9:16` một lần nữa. Đây là một bộ giảm cấp responsive cho màn hình dọc, không phải giao diện độc lập màn hình ngang điện thoại.
- `App.vue::resolveBrowserFullscreenContext() → toggleBrowserFullscreen()` chỉ gọi `requestFullscreen()` chuẩn của iframe; `bindBrowserFullscreen()` sẽ vô hiệu hóa nút trực tiếp khi `fullscreenEnabled=false` hoặc thiếu API. Hiện tại không có chế độ giảm cấp "mở rộng trong viewport nội dung trình duyệt", cũng không có đường xử lý giữ layout immersive khi Safari điện thoại/WebView hạn chế/người dùng từ chối fullscreen.
- Canvas gốc và một số popup dùng phép tính `100vh`/tỷ lệ cố định 16:9, không dùng `dvh/svh`, `visualViewport` và `env(safe-area-inset-*)` để xử lý thanh địa chỉ điện thoại, thanh dưới, tai thỏ/lỗ đục và hai hướng xoay màn hình ngang.
- Chuỗi gọi chính thức: host tầng 0 → `gameUi.ts::createTangquanGameUi()` tạo iframe game cùng nguồn → `syncInlineFrameSize()` cố định kích thước iframe → canvas gốc `App.vue` lại cố định tỷ lệ khung hình → các component con 3 chế độ chơi sắp xếp theo media query desktop/dọc → `toggleBrowserFullscreen()` thử giao iframe game cho Fullscreen API.

### P6 căn cứ thiết kế và phương án màn hình ngang độc lập

- Tham khảo sản phẩm thành công: player màn hình ngang của YouTube/Netflix dùng kết cấu immersive "nội dung ưu tiên, điều khiển đặt ở rìa, phủ lên khi cần"; các game di động màn hình ngang trưởng thành như Genshin Impact giữ hình ảnh chính ở trung tâm, đặt trạng thái ở trên trái/trái, thao tác chính ở vùng ngón cái bên phải, hội thoại và thao tác tần suất thấp ở dưới cùng.
- Căn cứ nền tảng: Apple HIG yêu cầu game màn hình ngang hiển thị toàn màn hình nhưng tránh safe area, và khuyến nghị vùng chạm mặc định iOS 44×44pt; Android chính thức yêu cầu tối thiểu 48×48dp, và nhấn mạnh system bar, lỗ đục và inset bàn phím ảo sẽ thay đổi khi chạy; web di động dùng `dvh/svh` để xử lý mở/thu toolbar trình duyệt, không thể dùng `vh` cố định để giả làm chiều cao khả dụng nữa.
- Nhận diện thiết bị: dựa vào viewport CSS thực tế, cạnh ngắn, khả năng chạm/con trỏ thô và hướng để cùng phán đoán là điện thoại; không phân nhánh theo chiều rộng đơn lẻ hay User-Agent. Điện thoại màn hình dọc chỉ hiển thị cổng xoay ngang rõ ràng, không render bộ giao diện game dọc thứ hai.
- Layout độc lập: thêm tầng logic viewport và style màn hình ngang điện thoại riêng. Trang tiêu đề dùng thương hiệu/không khí bên trái, thao tác chính vùng chạm lớn bên phải; trang chơi dùng thanh hệ thống tinh gọn trên cùng, trạng thái bên trái, cảnh giữa, cột thao tác ngón cái bên phải, tầng hội thoại dưới cùng; menu dùng track điều hướng cố định + vùng nội dung có thể cuộn, thay vì xếp chồng dọc các card desktop.
- Fullscreen trong trình duyệt: ưu tiên yêu cầu Fullscreen API chuẩn; khi thiếu API, bị từ chối hoặc bị host hạn chế, giảm cấp thành chế độ immersive cùng trang chỉ phủ `visualViewport` hiện tại của trình duyệt. Cả hai đường đều lấy viewport CSS thực tế của trình duyệt làm chuẩn, không tuyên bố tương đương fullscreen vật lý thiết bị, cũng không thay đổi save, trạng thái chơi hay trạng thái AI.
- Ma trận test chính xác: giả lập thiết bị Chrome DevTools dùng `mobile + touch + landscape`, bao phủ 740×360, 844×390, 915×412, 932×430 CSS px, và thêm phiên bản chiều cao thấp sau khi trừ phần toolbar trình duyệt cho mỗi nhóm; đồng thời ghi lại DPR, `innerWidth/innerHeight`, `visualViewport` và kích thước iframe/root thực tế.

### P6 file đã sửa và kết quả triển khai thực tế

- `ui/app/mobileViewport.ts`: thêm phân loại cổng xoay ngang/dọc/desktop 3 trạng thái cho điện thoại màn hình ngang, 8 nhóm fixture viewport có thể lặp lại, đọc kích thước sự thật `visualViewport` và xuất biến CSS.
- `ui/app/mobileLandscape.scss`: thêm giao diện màn hình ngang độc lập được cách ly hoàn toàn bởi `data-viewport-mode='mobile-landscape'`; trang tiêu đề là thương hiệu trái/thao tác chính phải, panel con là vùng làm việc toàn màn hình, 3 chế độ chơi là trạng thái trái/cảnh giữa/thao tác phải/hội thoại dưới, menu thống nhất thành track điều hướng trái + nội dung cuộn phải; xử lý vùng chạm 44/48px, safe area, chiều cao thấp, vùng thao tác cố định cho save, chiều cao HUD động và box biên menu.
- `ui/app/gameUi.ts`: iframe game chính thức đổi sang dùng `visualViewport` của window cha, phân loại chung theo chạm và hướng; iframe màn hình ngang dùng chiều cao nội dung trình duyệt thực tế, màn hình dọc vào cổng xoay ngang, desktop vẫn giữ 16:9 gốc; lắng nghe resize/orientation/visualViewport resize/scroll, `srcdoc` thêm `viewport-fit=cover`.
- `ui/app/App.vue`: giao diện gốc kết nối chế độ viewport và biến CSS động; thêm nút quay lại cho panel con màn hình ngang; fullscreen ưu tiên yêu cầu Fullscreen API chuẩn của iframe game hiện tại, khi bị từ chối/thiếu thì cố định phủ `visualViewport` hiện tại, và snapshot/khôi phục iframe, `#chat`, `#sheld` và overflow tài liệu. Giảm cấp không gắn lại iframe, nên không reload Vue hay mất trạng thái chạy.
- `scripts/test_tangquan_p6_mobile_landscape.cjs`: thêm hồi quy phân loại 8 nhóm viewport và ràng buộc mã nguồn, bao phủ cổng xoay ngang, ranh giới chạm/desktop, visualViewport, viewport-fit, safe area, kích thước chạm, track/box biên menu, cuộn save, chiều cao HUD và cấm `scale()` toàn trang.
- Không thay đổi schema save, MVU, chính văn world book, prompt/queue AI, công thức kinh tế hay ánh xạ ảnh minh họa/ảnh đại diện; không cần migration, ranh giới tương thích save cũ không đổi.

### P6 kết quả tự động hóa, kiểm tra kiểu và build

- Chuyên mục P6: `node scripts/test_tangquan_p6_mobile_landscape.cjs` pass, cả 8 nhóm fixture đều pass; ESLint có định hướng pass; Prettier của file đã sửa pass.
- Toàn bộ hồi quy cốt lõi pass: phân giải tin nhắn AI, tự động lưu, lưu/rollback world book, báo cáo ngày của chủ tiệm, ma trận kinh doanh chuyên sâu của chủ tiệm, sắp xếp hàng ngày và queue preset duy nhất, chợ nhân tài AI, migration save cũ của mascot, làm mới tuyển dụng, thời gian xếp ca, 3 chế độ chơi nhảy thời gian thủ công, AIRP song chế độ và ảnh minh họa OC thường. Test kinh doanh chuyên sâu của chủ tiệm tiếp tục bao phủ kịch bản 12×90 ngày, kinh doanh cực đoan 160 ngày, save/load JSON qua lại; stress test world book `maxConcurrent=1`.
- Kiểm tra kiểu toàn repo `pnpm exec tsc --noEmit --pretty false` đã thực sự chạy nhưng fail; fail do khai báo toàn cục có sẵn của repo và nhiều module không liên quan, Hoa Chưa Nở cũng có nợ kiểu như `customerGame.ts`/`waiterGame.ts` từ trước vòng này. Hai file `.ts` mới/đã sửa trong vòng này không xuất hiện trong danh sách lỗi, ESLint có định hướng và biên dịch TypeScript webpack chính thức đều pass; giữ nguyên ghi nhận lỗi, không sửa vượt phạm vi.
- Build dev chính thức `pnpm build:dev` pass; bundle tiếng Trung cuối cùng là 9,073,807 bytes, SHA256 `50197DB828B2CE03F20884E118FCE3ED71FD698993E3582D3194AE57A85A5A48`.
- Đối chiếu nội dung bundle: có chứa `mobile-landscape`, `visualViewport`, `viewport-fit=cover`; không chứa `127.0.0.1:8879`, ký hiệu QA của P6 hay chuỗi công cụ test tiếng Việt đã bị dọn.

### P6 nghiệm thu máy thật Chrome ngoài và dọn dẹp

- Chỉ điều khiển `http://127.0.0.1:8000/` mà người dùng đã mở sẵn; Chrome giả lập chính xác `mobile + touch + DPR 3`. Test thực tế màn hình ngang ở 740×320, 844×390, 932×430, giao diện gốc ở trạng thái immersive khớp chính xác từng nhóm với viewport nội dung CSS; 390×844 màn hình dọc chỉ hiển thị cổng xoay ngang.
- Fullscreen API gốc ở 844×390: iframe game/root đều là 844×390, toolbar và vùng nhập của Tavern không còn che khuất. Sau khi ép `requestFullscreen()` bị từ chối, trạng thái giảm cấp cũng là 844×390, ở tầng trên cùng, giữ cùng `contentWindow`; sau khi thoát, style/node cha gốc của iframe, z-index của `#chat`/`#sheld` và overflow của HTML đều được khôi phục từng mục.
- Save 12 slot: ở 844×390 cả 3 hàng đều hiển thị đầy đủ, vùng thao tác cố định; ở 740×320 danh sách sinh ra cuộn dọc độc lập 21px, cuộn xuống đáy thì slot thứ 12 hiển thị đầy đủ, vùng thao tác dưới cùng vẫn cố định và panel không tràn. Setting, chọn chế độ, nút quay lại và điều khiển cửa sổ góc trên phải không bị chồng lấp.
- 3 chế độ chơi hoàn thành nghiệm thu hình ảnh bằng trạng thái memory của Vue, không tạo save, không gọi AI, không ghi world book: màn hình chính, menu, trang danh bạ/đầu tư, input AIRP, chọn thời gian và tầng hướng dẫn của chủ tiệm, khách, nhân viên đều hiển thị theo cấu trúc màn hình ngang độc lập; menu khách/nhân viên cuối cùng đã được sửa từ 884×426 vượt giới hạn thành chính xác 844×390.
- Save thật của người dùng giống hệt từng byte từ trước khi inject đến sau khi dọn dẹp: `slot-01` 22,588 bytes / `b553d947` / `2026-07-16T12:11:56.399Z`; `slot-02` 11,226 bytes / `a611f718` / `2026-07-16T12:12:13.569Z`. Không có save nào được tạo mới, load, ghi đè hay xóa.
- Kết quả dọn dẹp: trạng thái memory Vue của QA đã khôi phục, frontend tạm đã unmount; iframe/host game, object toàn cục P6/QA, fallback dataset, trạng thái fullscreen và z-index tạm của `#chat`/`#sheld` đều về 0/rỗng; giả lập thiết bị khôi phục về 2560×1223, DPR 1, không touch/coarse; 8879 đã dừng và xác nhận `NO_LISTENER`. Không có save test hay entry world book test nào cần xóa.

### P6 vấn đề chưa giải quyết và trạng thái phát hành

- Chưa nghiệm thu trên thiết bị vật lý iPhone/Safari hay trình duyệt Android thật; vòng này hoàn thành theo viewport nội dung trình duyệt chính xác của Chrome mà người dùng cho phép. Hợp đồng safe area và viewport động đã triển khai, nhưng thông báo không được viết thành "đã bao phủ toàn bộ điện thoại vật lý".
- Kiểm tra kiểu TypeScript toàn phần cấp repo vẫn bị chặn bởi nợ kiểu lịch sử, xem ghi nhận lỗi ở trên; không ảnh hưởng kết luận build webpack và kiểm tra có định hướng của vòng này.
- Vòng này không có commit, push, tag, upload GitHub/CDN hay sửa link online; bundle cuối cùng chỉ là ứng viên cục bộ cho phiên bản tiếp theo.

## Task mới thêm vòng này: P7 giả lập điện thoại Android và môi trường debug từ xa trên ổ D

### Yêu cầu gốc của người dùng

1. Cài đặt trực tiếp môi trường giả lập điện thoại để dùng cho việc thích ứng điện thoại sau này.
2. Nội dung cài đặt đặt vào ổ D và tạo thư mục độc lập; ổ này dùng để lưu trữ công cụ và dữ liệu lâu dài.

### Mục tiêu cập nhật, phạm vi ảnh hưởng và tiêu chí nghiệm thu ban đầu

- Thư mục mục tiêu: `D:\AndroidDev`; dùng đường dẫn ASCII, không dấu cách, giảm rủi ro tương thích đường dẫn cho Android SDK, AVD, Gradle và command-line tools.
- Dự kiến cài đặt: Android Studio, Android SDK, Platform-Tools, Android Emulator, system image Google Play/Google APIs ổn định và AVD `Tangquan_QA` chuyên dụng; phiên bản cụ thể lấy theo kênh ổn định chính thức hiện tại và khả năng tương thích của máy.
- Ranh giới persist: SDK, AVD, cache download và cache dự án sau này cố gắng cố định trên ổ D; không di chuyển hay xóa file Android/Java/IDE hiện có của người dùng, không sửa Git config toàn cục.
- Ảnh hưởng dự án: task này chỉ xây dựng toolchain QA cục bộ, không sửa save game, MVU, world book, AI, hệ thống kinh tế, chính văn character card hay bundle chính thức; không ghi vào thông báo phiên bản game tiếp theo.
- [x] Đã ghi nhận yêu cầu gốc, thư mục mục tiêu, phạm vi ảnh hưởng và ranh giới cài đặt của người dùng.
- [x] Đã kiểm tra dung lượng khả dụng ổ D, công cụ Android hiện có, ảo hóa Windows và dependency cài đặt.
- [x] Đã cài Android Studio, SDK, Platform-Tools và Emulator, ghi lại phiên bản, thư mục và biến môi trường chính xác.
- [x] Đã tạo và khởi động AVD màn hình ngang `Tangquan_QA`; xác nhận Chrome chạy được, xoay được, dùng được bàn phím ảo và giữ dữ liệu QA độc lập.
- [x] Đã xác minh `adb devices`, reverse port 8000/8879, cổng debug CDP Chrome Android và truy cập trang web host.
- [x] Đã xác nhận Android SDK/AVD/cache thực sự nằm trên ổ D, không lỡ cài system image lớn của vòng này vào ổ C.
- [x] Đã bổ sung lệnh cài đặt, thư mục thực tế, phiên bản, kết quả xác minh, ghi nhận lỗi, hạn chế còn lại và cách dùng sau này.

### Tính tương thích, nghiệm thu ngoài và đánh dấu phát hành

- Save/MVU/world book/AI/kinh tế/save cũ: không ảnh hưởng.
- Chrome ngoài: đối tượng xác minh của task này đổi thành Chrome trong Android Emulator; không ghi đè trang hay dữ liệu hiện có của Chrome desktop người dùng.
- Thông báo phiên bản tiếp theo: không cần thiết; đây là hạ tầng dev/QA cục bộ, không phải cập nhật nội dung game.
- Trạng thái hiện tại: môi trường QA màn hình ngang Android cục bộ đã cài đặt, khởi động và pass xác minh kết nối/viewport/chạm/bàn phím ảo; UI màn hình ngang điện thoại của game chưa bắt đầu thiết kế hay sửa đổi.

### Kết quả cài đặt thực tế

- Thư mục gốc cài đặt: `D:\AndroidDev`; sau khi cài, ổ D còn lại khoảng `1217.12 GiB`.
- Android Studio: gói cài đặt ổn định chính thức hiện tại `2026.1.2.10`, gói cài đặt được giữ tại `D:\AndroidDev\Installers\`, SHA256 là `938FD37C6029C2BDFF8B62BED4FB8C87A260BEBE2DC7DA12D3F79B3AF432829E`; cổng chạy trên ổ D là `D:\AndroidDev\AndroidStudio\bin\studio64.exe`, mã build sản phẩm là `AI-261.25134.95.2612.15822958`.
- SDK: `D:\AndroidDev\Sdk`; Platform-Tools / adb `37.0.0-14910828`, Emulator `36.6.11.0 (15507667)`, Command-line Tools `22.0`, Build-Tools `36.0.0`, Android 36 Platform revision 2, Google Play x86_64 Android 36 system image revision 7.
- AVD: `Tangquan_QA`, cấu hình Pixel 8, API 36 / Android 16, Google Play x86_64; AVD và snapshot cố định tại `D:\AndroidDev\Avd`. Panel vật lý `1080 × 2400 px`, `420 dpi`, script khởi động cố định thành `2400 × 1080 px` màn hình ngang, RAM `4096 MiB`, phân vùng data `10 GiB`.
- Dung lượng logic thư mục ổ D: Android Studio `3.92 GiB`, SDK `3.65 GiB`, AVD `15.69 GiB`; AVD chứa image đĩa cấp phát trước/sparse, dung lượng logic không bằng dung lượng chiếm dụng thực tế.
- Biến môi trường người dùng đã thiết lập: `ANDROID_HOME` / `ANDROID_SDK_ROOT = D:\AndroidDev\Sdk`, `ANDROID_USER_HOME = D:\AndroidDev\UserHome`, `ANDROID_AVD_HOME = D:\AndroidDev\Avd`, `GRADLE_USER_HOME = D:\AndroidDev\Gradle`; Path người dùng đã thêm Studio, platform-tools, emulator và cmdline-tools. Terminal mới sẽ tự động kế thừa.
- Script tắt: `D:\AndroidDev\Start-Tangquan-QA.cmd` khởi động và khôi phục trang Chrome duy nhất; `D:\AndroidDev\Stop-Tangquan-QA.cmd` dừng AVD đó và gỡ port mapping; `D:\AndroidDev\README.md` ghi cách dùng hàng ngày và baseline thiết bị hiện tại.

### Chuỗi gọi thực tế và kết quả nghiệm thu

- Chuỗi gọi trang host: Chrome Android yêu cầu `http://127.0.0.1:8000/` → `adb reverse tcp:8000 tcp:8000` → SillyTavern đã chạy sẵn của người dùng trên máy tính; đã thấy trang SillyTavern chính thức và không sửa Chrome desktop.
- Chuỗi debug bundle: Chrome Android yêu cầu emulator `127.0.0.1:8879` → thiết lập theo nhu cầu `adb reverse tcp:8879 tcp:8879` → service không cache/CORS tạm trên máy tính. Vòng này chỉ xác minh đọc, emulator thực sự lấy về bundle trên đĩa hiện tại `9,073,807 bytes`, SHA256 `50197DB828B2CE03F20884E118FCE3ED71FD698993E3582D3194AE57A85A5A48`; chưa thực hiện build, chưa inject bundle vào character card, chưa coi số liệu đọc này là phiên bản phát hành mới.
- Chuỗi debug: máy tính `http://127.0.0.1:9223/json` → `adb forward tcp:9223 localabstract:chrome_devtools_remote` → CDP Chrome Android; Chrome desktop đã chiếm 9222, nên cổng chuyên dụng cố định là 9223.
- `adb devices` cuối cùng ổn định trả về `emulator-5554 device`, `sys.boot_completed=1`; script khởi động chạy lặp lại vẫn chỉ có một tab `SillyTavern`, không sinh tab trùng lặp.
- Phiên bản Chrome Android tích hợp là `133.0.6943.137`. CDP thực đo `screen = 915 × 412 CSS px`, DPR `2.625`, chạm 5 điểm, `landscape-primary / 90°`; khi thanh địa chỉ mở rộng `visualViewport` khoảng `915 × 287 CSS px`, khi thanh địa chỉ thu gọn khoảng `915 × 308 CSS px`. Thiết kế màn hình ngang sau này bắt buộc phải bao phủ cả hai chiều cao "fullscreen trong trình duyệt" này, không được chỉ sắp xếp theo màn hình vật lý `2400 × 1080`.
- Bàn phím ảo Gboard màn hình ngang đã thực sự bật lên, `mInputShown=true`, `mIsInputViewShown=true`; có thể dùng để xác minh sau này về ô nhập, nén viewport hình ảnh và né vùng thao tác dưới cùng.
- Mạng ngoài của AVD chỉ dùng `10.0.2.2:10808` khi host `127.0.0.1:10808` tồn tại, và loại trừ `127.0.0.1` / `localhost` / `10.0.2.2`; không sửa proxy toàn cục của Windows. Kết nối CDN ngoài đã pass.
- Xác minh tự động hóa/công cụ: khởi động, khởi động lặp lại, dừng, cold start, ép màn hình ngang, truy cập trang host, liệt kê trang CDP, đọc viewport, khai báo chạm, bàn phím ảo, mạng ngoài, reverse 8000/8879, forward 9223 đều đã xác minh từng mục.
- ESLint / TypeScript / kiểm tra kiểu Vue / `pnpm build:dev`: vòng này không chạy; lý do là không sửa bất kỳ mã nguồn game, dữ liệu character card hay entry build nào. Bundle chính thức chỉ đối chiếu số byte/SHA256 chỉ-đọc và round-trip mạng emulator.

### Ghi nhận lỗi, hạn chế và dọn dẹp

- Sau khi installer Nullsoft chính thức phát hiện ổ C đã có Android Studio đăng ký, nó bỏ qua target `/D` và nâng cấp bản gốc lên 2026.1.2 trước; không xóa, di chuyển hay ghi đè setting của người dùng. Sau đó IDE hoàn chỉnh đã nâng cấp được copy sang `D:\AndroidDev\AndroidStudio` làm cổng vào trên ổ D. Studio gốc trên ổ C vẫn do installer chính thức quản lý, không thể tự ý xóa; SDK, system image, AVD và cache mới của vòng này đều ở ổ D.
- `avdmanager` khi tạo từng báo không đọc được `devices.xml`, nhưng file AVD đã tạo thành công; sau đó đã đối chiếu và bổ sung đầy đủ cấu hình Play Store, GPU, màn hình ngang, bàn phím ảo, RAM, độ phân giải và data, cold start pass.
- Script khởi động PowerShell phiên bản đầu chứa tiếng Trung bị Windows PowerShell 5 phân tích sai encoding; đã đổi text của script thực thi sang ASCII. Phiên bản đầu dùng `monkey` để khôi phục Chrome sẽ mở lại tự động xoay; đã đổi thành khởi động rõ ràng Main Activity của Chrome, cuối cùng `accelerometer_rotation=0`, `user_rotation=1`.
- Chrome tích hợp trong system image Google Play hiện tại là 133, không cùng phiên bản với Chrome desktop 2026. Chưa đăng nhập tài khoản Google người dùng, chưa dùng APK bên thứ ba; nếu sau này cần cổng "tương thích engine Chrome mới nhất", nên cập nhật qua Play Store trong tài khoản QA độc lập, hoặc xây dựng thêm thiết bị test trình duyệt phiên bản mới.
- Kết quả dọn dẹp: service Node 8879 tạm đã dừng và xác nhận cổng không còn listener, 16 file screenshot/chẩn đoán tạm cục bộ và screenshot tương ứng trong emulator đã xóa, reverse 8879 đã gỡ; chưa tạo save test, entry world book, dữ liệu MVU, inject toàn cục hay dữ liệu character card. Để tiện người dùng xem ngay, đã giữ lại `Tangquan_QA` đang chạy, reverse 8000 và CDP 9223; bấm đúp script dừng có thể tắt hoàn toàn.
- Trạng thái phát hành: không có thao tác commit, push, tag, CDN, link online hay thông báo phiên bản; task này không ảnh hưởng thông báo phiên bản game tiếp theo.

## Task mới thêm vòng này: P8 "Mở Hoa Chưa Nở" trong giả lập Android không phản hồi

### Yêu cầu gốc và hiện tượng vấn đề của người dùng

- Người dùng mở `http://127.0.0.1:8000/` trong Chrome Android màn hình ngang `Tangquan_QA`, đã chọn character card "Hoa Chưa Nở"; bấm nút "Mở Hoa Chưa Nở" ở dưới cùng không có phản ứng gì.
- Trong screenshot nút hiển thị màu xám, cần xác nhận trước là `disabled` thật, script/event listener cổng vào chưa load, nhận diện nhân vật fail, gọi cross-frame fail, hay vấn đề đường chạm Android, không được dùng bấm cứng theo tọa độ để che giấu.

### Phạm vi ảnh hưởng và tiêu chí nghiệm thu ban đầu

- Phạm vi ảnh hưởng: cổng vào character card chính thức, chuỗi gọi load trang host và frontend tầng 0 trong Chrome Android; trước khi làm rõ không giả định ảnh hưởng đến Chrome desktop.
- Save/MVU/world book/AI/kinh tế/save cũ: hiện tại chỉ xảy ra ở giai đoạn mở cổng vào, dự kiến không nên ghi đè các nguồn sự thật này; trong lúc xác minh cấm ghi đè save thật của người dùng hoặc tạo mảnh vỡ world book runtime.
- Chrome ngoài: cần dùng Chrome Android `Tangquan_QA` đã cài đặt để tái hiện và nghiệm thu máy thật; không thao tác trang Chrome desktop của người dùng.
- Thông báo phiên bản tiếp theo: cần ghi "sửa lỗi một số trình duyệt bấm 'Mở Hoa Chưa Nở' không phản hồi"; mã nguồn chính thức tồn tại lỗ hổng tương thích khởi động Pinia ESM ngoài.
- [x] Đã đọc trang hiện tại của Chrome Android, thuộc tính DOM của nút, event listener, lỗi console và trạng thái load script cổng vào chính thức.
- [x] Đã tái hiện ổn định việc bấm không phản hồi, và làm rõ chuỗi gọi thực tế trang host → nút cổng vào → frontend tầng 0 cùng nguyên nhân gốc.
- [x] Đã triển khai sửa mã nguồn chính thức tối thiểu và hoàn chỉnh, không hard-code click, không bypass cổng vào chính thức.
- [x] Đã bổ sung xác minh hồi quy fail trước khi sửa, pass sau khi sửa; đóng/mở lặp lại và bấm lặp lại đều chỉ giữ một instance.
- [x] Đã dùng CDN chính thức `tq-v1.0.4` để hoàn thành nghiệm thu refresh toàn trang, chọn lại "Hoa Chưa Nở", cold start script nhân vật và chạm thật "Mở Hoa Chưa Nở"; controller chính thức, iframe game duy nhất và trang tiêu đề màn hình ngang điện thoại đều được thiết lập bình thường.
- [x] Chrome Android đã dùng CDP `Input.dispatchTouchEvent` chạm thật mở thành công, xác nhận tầng 0, host và iframe game mỗi loại chỉ có một và không có save/world book/MVU/dữ liệu AI mới nào được thêm.
- [x] Đã bổ sung file đã sửa, kết quả test/kiểm tra/build, số byte/SHA256 bundle, hạn chế, dọn dẹp và trạng thái phát hành.
- Trạng thái hiện tại: mã nguồn chính thức và phiên đang chạy hiện tại của emulator đã sửa và pass nghiệm thu chạm Android; refresh vĩnh viễn/cold start vẫn chờ sản phẩm build phiên bản tiếp theo được phép phát hành.

### Nguyên nhân gốc và chuỗi gọi thực tế

- Nút mà người dùng thấy ban đầu không phải DOM bị disabled: nó là `<div role="button" class="qr--button ...">`, `pointer-events:auto`, bản thân việc chạm trúng vẫn bình thường. Sau khi thực thi `click()` thật của nút trước khi sửa, `window.tangquanFrontend`, `window.tangquanZeroLayer`, iframe script chính thức và UI host vẫn hoàn toàn không tồn tại, tái hiện ổn định việc không phản hồi.
- Nguyên nhân tầng đầu là cấu hình runtime bị lệch vị trí: script nhân vật thật `未开之花 / ce05002f-5949-4b3e-8e9c-37eaf5943f41` là `enabled:false`; nút duy nhất cùng tên trên trang đến từ cấu hình nút còn sót lại sau khi trước đó inject bundle chính thức vào iframe `同步 / e5c7aecf-759f-4644-8da1-b2e6f0afed3f` toàn cục. Nút vỏ rỗng này không có handler sự kiện Hoa Chưa Nở.
- Sau khi bật script nhân vật đúng lại lộ ra lỗ hổng mã chính thức tầng thứ hai: script nhân vật lấy về thành công bundle `tq-v1.0.3` `8,808,892 bytes`, nhưng `pinia/+esm` bên ngoài mà jsDelivr chuyển đổi hiện tại đọc trực tiếp biến tự do chưa định nghĩa khi `createPinia()`, ném ra `ReferenceError: __VUE_PROD_DEVTOOLS__ is not defined`; khởi tạo dừng lại trước khi đăng ký listener nút và expose controller.
- Chuỗi gọi thực tế là: nút QR của Tavern → sự kiện nút của script Tavern Helper tương ứng → `index.ts::initializeTangquanFrontend()` → `createPinia()` → tạo các service tầng 0/save/MVU/world book → `eventOn(getButtonEvent('打开未开之花'))` → `gameUi.open()`. Lỗi lần này xảy ra ở `createPinia()`, nên các listener và UI sau đó đều chưa được tạo.
- Lỗi reconnect WebSocket của TavernSync với `localhost:6620/6621` cũng tồn tại đồng thời, nhưng chỉ thuộc về nhiễu do chưa khởi động local watch service, không phải nguyên nhân gốc của BUG này.

### Sửa lỗi thực tế và file đã sửa

- `src/汤泉/脚本/零层美化/index.ts`: trước khi DOM-ready khởi động và lần `createPinia()` đầu tiên, chỉ khi host chưa cung cấp mới set `globalThis['__VUE_PROD_DEVTOOLS__']` của realm iframe script Tavern Helper hiện tại thành `false`. Dùng computed property để tránh `DefinePlugin` của webpack coi gán tương thích host là hằng số biên dịch của bundle này; không ghi đè giá trị host đã có.
- `scripts/test_tangquan_p8_runtime_bootstrap.cjs`: thêm hồi quy thứ tự khởi động, ép kiểm tra thiếu flag tương thích, thứ tự gán và thực thi phải sớm hơn DOM-ready và `createPinia()`.
- `src/汤泉/二期体验升级清单.md`: ghi nhận và bổ sung BUG này.
- Runtime chỉ thực hiện hai sửa cấu hình chính xác: đặt đúng script nhân vật Hoa Chưa Nở thành enabled; từ script `同步` toàn cục chỉ xóa nút "Mở Hoa Chưa Nở" còn sót lại, giữ nguyên trạng thái enabled, nội dung, 6 nút ẩn khác và dữ liệu không đổi. Sau khi dọn dẹp, `getAllEnabledScriptButtons()` chỉ có nút đó thuộc về script nhân vật đúng.
- Không sửa save, entry world book, queue AI, kinh tế, trạng thái chơi hay chính văn nhân vật; hook chuẩn `pnpm build:dev` vẫn đóng gói lại bình thường `未开之花.json` và `明月秋青Maya.json` hiện có, không viết tay lại chính văn của hai file này.

### Hồi quy, kiểm tra, build và nghiệm thu Android

- Hồi quy trước khi sửa: `scripts/test_tangquan_p8_runtime_bootstrap.cjs` fail với lý do "phải cung cấp flag runtime production Vue còn thiếu cho Pinia bên ngoài"; sau khi sửa test tương tự pass.
- `scripts/test_tangquan_p6_mobile_landscape.cjs`: pass, cả 8 fixture viewport màn hình ngang chính xác vẫn vào layout `mobile-landscape` độc lập.
- ESLint có định hướng: `index.ts` và script hồi quy P8 pass, không có output.
- Kiểm tra kiểu Vue: repo chưa cài `vue-tsc`, lệnh fail rõ ràng là `Command "vue-tsc" not found`; sau đó chạy `pnpm exec tsc --noEmit --pretty false` toàn repo, bị chặn bởi lỗi kiểu có sẵn của `TypeFest` / thiếu kiểu Node, Trí Não, NAI và trạng thái có sẵn của Hoa Chưa Nở, output không có lỗi mới nào của `index.ts` được sửa lần này. Giữ nguyên ghi nhận lỗi, không tuyên bố kiểm tra kiểu toàn repo pass.
- Build dev chính thức: lần đầu gặp `UNKNOWN open` của Windows khi ghi vào `dist/汤泉/脚本/零层美化/index.js` có sẵn; sau khi xác nhận file đọc/ghi được, không bị chiếm dụng 8879/6620/6621, thử lại với lệnh gốc thì webpack biên dịch thành công entry chính thức của Hoa Chưa Nở và toàn bộ entry của dự án.
- Bundle tiếng Trung cục bộ cuối cùng: `9,077,733 bytes`, SHA256 `AAB007F6D1940CC7D46083E2A290F41D044F172F7B579F005071C3767F8A77AC`; sản phẩm build có chứa flag tương thích. Chrome Android lấy về kết quả qua đường 8879 no-store/CORS cũng là HTTP 200, `9,077,733 bytes` và SHA256 hoàn toàn giống nhau.
- Sau khi bundle chính thức cục bộ được load trong iframe script Tavern Helper của script nhân vật đúng, `window.tangquanFrontend`, `window.tangquanZeroLayer`, gỡ khẩn cấp, host và tầng 0 hình ảnh đều tồn tại; tiêu đề iframe game là "未开之花" (Hoa Chưa Nở), tiêu đề trang chính thức, bắt đầu/tiếp tục/save/setting đều render, chế độ viewport là `mobile-landscape`.
- Sau khi đóng giao diện, dùng CDP `Input.dispatchTouchEvent` của Chrome Android gửi chạm thật tại trung tâm nút `(438.77, 231.36 CSS px)`, giao diện mở lại; sau đó bấm lặp lại vẫn là `gameFrames=1`, `hosts=1`, `visualZero=1`, `openButtons=1`.

### Dọn dẹp, hạn chế và trạng thái phát hành

- Service tạm 8879 đã dừng và xác nhận không còn listener, reverse 8879 đã gỡ; script chẩn đoán, screenshot và XML trên ổ D đã dọn về 0, file tương ứng trong emulator đã xóa; loader cục bộ `<script data-tq-local-test>` đã gỡ. Để phản hồi thao tác hiện tại của người dùng, giữ lại một instance UI chính thức đã load xong duy nhất và reverse 8000.
- Không tạo hay ghi đè save, không thêm/bật/xóa entry world book, không kích hoạt ghi MVU, yêu cầu AI, trừ tiền hay tin nhắn楼层; chỉ sửa đúng cấu hình enabled/nút của hai script Tavern Helper nói trên.
- Hạn chế: script nhân vật hiện vẫn trỏ đến `tq-v1.0.3` đã phát hành, file CDN đó không chứa sửa mã nguồn lần này. Phiên cục bộ đã load hiện tại có thể dùng bình thường, nhưng refresh trang, chọn lại card hay cold start sẽ load lại CDN cũ, không thể tuyên bố đã sửa vĩnh viễn trước khi phát hành.
- Trạng thái phát hành: không có thao tác commit, push, tag, CDN hay link online. Khi phát hành phiên bản tiếp theo phải build/hồi quy lại, và ghi BUG này cùng với "sửa nền ảnh minh họa nhân vật chưa được tách" có sẵn vào thông báo.

## Task mới thêm vòng này: P9 audit hình ảnh màn hình ngang và phúc tra thiết kế trên Chrome Android thật

### Yêu cầu gốc và mục tiêu của người dùng

- Người dùng xác nhận bắt đầu vòng audit hiện trạng màn hình ngang điện thoại và phương án thiết kế đầu tiên; người dùng chịu trách nhiệm xem hiệu quả cuối cùng và quyết định hướng thẩm mỹ, thao tác giả lập, screenshot, sửa và test do Codex thực hiện.
- Vòng này không phải chạy lại P6 đã hoàn thành, mà là dùng Chrome Android thật `Tangquan_QA` mới cài đặt, thực hiện vòng audit hình ảnh trên trình duyệt thật thứ hai đối với bản ứng viên cục bộ của P6, tìm ra các vấn đề về thanh trình duyệt, chạm, font, trạng thái nhúng và trạng thái immersive mà giả lập Chrome chính xác cũ chưa bao phủ.
- Phúc tra thiết kế bắt buộc phải tham khảo các App/game di động màn hình ngang vẫn đang thành công, chỉ hấp thụ kiến trúc thông tin, vùng nóng ngón tay, ưu tiên nội dung và nguyên tắc điều khiển immersive đã trưởng thành, không copy tư liệu thương hiệu hay thay đổi văn phong sẵn có của "Hoa Chưa Nở".

### Phạm vi ảnh hưởng và tiêu chí nghiệm thu ban đầu

- Cổng vào chính thức: `src/汤泉/脚本/零层美化/`; đối tượng audit là trạng thái chạy thực tế của bundle chính thức cục bộ phiên bản tiếp theo hiện tại trong Chrome Android, không dùng HTML preview.
- Ranh giới dữ liệu: vòng audit đầu tiên chỉ điều hướng trang tiêu đề, chế độ/save/setting và các giao diện có thể tiếp cận an toàn; không được tạo, load, ghi đè hay xóa save của người dùng, không kích hoạt AI, world book, MVU, quyết toán kinh tế hay tự động lưu. Các trang sâu của 3 chế độ chơi chỉ được dùng trạng thái memory thuần túy có thể rollback, không thể chứng minh an toàn thì để lại nghiệm thu chuyên biệt sau này.
- Nghiệm thu ngoài: dùng Chrome Android `Tangquan_QA`, ghi lại màn hình vật lý, CSS screen, `innerHeight` / `visualViewport`, mở/thu thanh trình duyệt, trạng thái nhúng và trạng thái immersive viewport nội dung trình duyệt; không thao tác Chrome desktop.
- Thông báo phiên bản tiếp theo: vòng audit thuần túy này không viết thông báo riêng; nếu sau này sửa layout chính thức theo audit thì gộp vào mục thông báo "giao diện độc lập màn hình ngang điện thoại" có sẵn.
- [x] Đã ghi nhận yêu cầu gốc, phạm vi, ranh giới dữ liệu và mục tiêu nghiệm thu của vòng audit Android thật lần hai.
- [x] Đã ghi lại Chrome Android hiện tại, bundle ứng viên cục bộ P6/P8, viewport và baseline instance đang chạy.
- [x] Đã chụp và audit trang tiêu đề, chọn chế độ, save, setting, điều khiển fullscreen/immersive trong trình duyệt và cổng xoay ngang.
- [x] Trong điều kiện không động vào save thật, đã audit màn hình chính và menu chính của chủ tiệm, khách, nhân viên; phần không thể tiếp cận an toàn đã ghi rõ hạn chế.
- [x] Đã nghiên cứu pattern giao diện hiện tại của App/game màn hình ngang điện thoại thành công, và hình thành bản đồ thiết kế có thể truy vết.
- [x] Đã đưa ra ưu tiên vấn đề, nguyên tắc layout chuyên dụng cho điện thoại, phương án cải cách từng trang, ma trận nghiệm thu chạm/viewport động và các lựa chọn thẩm mỹ cần người dùng quyết định.
- [x] Đã dọn dẹp screenshot, object chẩn đoán, inject tạm và service, bổ sung kết quả audit và bước tiếp theo; trước khi kết thúc vòng audit đầu tiên không tự ý commit, push hay phát hành.
- Trạng thái hiện tại: vòng audit hiện trạng màn hình ngang và phúc tra thiết kế đầu tiên trên Chrome Android thật đã hoàn thành; vòng này không sửa mã nguồn layout chính thức, không build bundle mới, cũng không commit, push hay phát hành.

### P9 baseline máy thật và chuỗi gọi thực tế

- Thiết bị/trình duyệt: `Tangquan_QA` (`emulator-5554`), Chrome Android `133.0.6943.137`; màn hình vật lý `1080 × 2400`, sau khi ép màn hình ngang pixel thiết bị `2400 × 1080`, CSS `screen=915 × 412`, DPR `2.625`, `maxTouchPoints=5`, hướng `landscape-primary / 90°`.
- Baseline không fullscreen của trình duyệt: khi thanh địa chỉ Chrome và trang Tavern vẫn còn, trang chính `inner/visualViewport=915 × 308`; điểm bắt đầu khả kiến của iframe game chính thức trong trang host khoảng `y=152.79`, nên người chơi thực tế chỉ thấy dải game cao khoảng `155 CSS px`. Bản thân iframe báo cáo `841 × 308` không thể đại diện cho việc người chơi thực sự thấy đủ 308px; đây là nguồn sự thật của hiện tượng "trong trình duyệt nhìn đã là màn hình ngang, nhưng game vẫn không dùng được".
- Baseline fullscreen trong trình duyệt: `App.vue::resolveBrowserFullscreenContext()` lấy `frameElement` của tài liệu Vue hiện tại, `toggleBrowserFullscreen()` gọi `requestFullscreen()` chuẩn cho iframe chính thức đó; sau khi Chrome thành công, `document.fullscreenElement` chính là iframe đó, node gốc game và `visualViewport` đều khoảng `915 × 412`, không xảy ra refresh hay rebuild trạng thái. Lần đầu Chrome vào fullscreen sẽ hiển thị gợi ý giáo dục "Viewing full screen" của riêng trình duyệt, gợi ý này không thể bị style trang xóa bỏ.
- Bundle audit hiện tại: `dist/汤泉/脚本/零层美化/index.js` là `9,077,733 bytes`, SHA256 `AAB007F6D1940CC7D46083E2A290F41D044F172F7B579F005071C3767F8A77AC`; nó chứa bản sửa cục bộ P8 chưa phát hành, không bằng bản online `tq-v1.0.3`. Vòng này không build lại, nên số byte và hash không thay đổi.
- Instance đang chạy: luôn giữ 1 iframe frontend Vue chính thức; cả baseline và sau dọn dẹp đều là `activeSlotId='' / runtime.activeMode='未选择'`. Cả 12 slot trong Temporary Chat của Android đều rỗng, không có gọi tạo, load, save hay xóa save nào.

### P9 kết luận máy thật và ưu tiên vấn đề

- **P0: trạng thái nhúng không fullscreen thực tế không thể chơi được.** Chiều cao game khả kiến thực tế chỉ khoảng `155 CSS px`, trang tiêu đề chỉ còn một lát cắt ngang, nút fullscreen tuy tồn tại nhưng chi phí phát hiện cao. Nguyên nhân gốc không phải layout màn hình ngang Vue bị nén, mà là "thanh trình duyệt Chrome + tiêu đề/vùng nhập host Tavern + iframe chính thức" cùng cắt mất viewport nội dung. Vòng sau bắt buộc phải định nghĩa trạng thái không fullscreen thành tầng khởi động chuyên dụng, thay vì tiếp tục cố chạy game hoàn chỉnh trong dải hẹp này.
- **P0: tầng chọn mascot save mới của chủ tiệm tồn tại lỗi hợp thành GPU của Chrome Android.** `.hostess-picker-layer` là tầng toàn màn hình `position: fixed`, dùng `backdrop-filter: blur(10px)`; sau khi ảnh ứng viên load xong, được nâng lên tầng hợp thành, Chrome 133 thật sẽ xóa sạch chữ tiêu đề và card về mặt hình ảnh, nhưng DOM, màu, opacity và kích thước vẫn bình thường. Chỉ sau khi gỡ `backdrop-filter` của tầng đó ở trạng thái chẩn đoán, ảnh và toàn bộ chữ lập tức khôi phục cùng lúc, chứng minh nguồn sự thật là hợp thành mờ toàn màn hình đó, không phải font, tài nguyên hay trạng thái Vue. Sửa chính thức chỉ nên đổi sang opacity/gradient mask ở tầng màn hình ngang điện thoại, không được dựa vào delay, redraw hay hard-code chữ để che giấu.
- **P1: trọng tâm thông tin của trang chọn chế độ thiên xuống dưới.** 3 dải chế độ cao khoảng `86px` tập trung ở dưới cùng, nửa trên để trống quá lớn; vẫn dùng được nhưng không giống lần đầu chọn nhân vật/chế độ của game di động trưởng thành. Đề xuất nâng cấp 3 card chọn thành track hình ảnh ngang chiếm phần thân trung tâm, card được chọn cho ra trách nhiệm chế độ, nhịp chơi và hình ảnh chính, không thêm trường dữ liệu mới.
- **P1: popup chọn mascot của chủ tiệm cao vượt quá màn hình ngang gọn.** Dialog thực đo khoảng `880 × 388`, bên trong `scrollHeight=421 / clientHeight=387`; ngay cả khi sửa mờ, phía dưới vẫn còn cuộn nội bộ nhẹ. Nên nén tiêu đề thành một dòng thanh thông tin, đổi ứng viên thành dải card ngang, và để thao tác xác nhận/hủy cố định trong safe area.
- **P2: màu chọn của trang save không thống nhất với tông nâu vàng hiện tại.** 12 slot rỗng dạng `4 × 3` đều hiển thị đủ trong 1 màn hình, một slot khoảng `201 × 73`, kích thước chạm đạt chuẩn; nhưng màu highlight xanh ngọc quá sáng. Các lựa chọn chính trên trang setting đều cao khoảng `48px`, cũng hiển thị đủ trong 1 màn hình, không phải BUG khả dụng, chỉ cần thu gọn bảng màu và phân bổ khoảng trắng ở giai đoạn thống nhất hình ảnh.
- **Mục pass:** vào/thoát fullscreen trong trình duyệt, trang tiêu đề, trang setting, trang save, màn hình chính của chủ tiệm/khách/nhân viên và cả 3 bộ thanh bên đều chạy được trong `915 × 412`; máy thật màn hình dọc hiển thị đầy đủ cổng "vui lòng xoay ngang điện thoại", quay lại màn hình ngang thì trạng thái được giữ; các nút menu khả kiến hiện tại lấy mẫu đều đạt tối thiểu cao `48px`.
- **Hiện tượng chẩn đoán không tính là BUG sản phẩm:** một lần chạm hợp thành CDP sau khi chuyển trang rơi vào cùng tọa độ với slot save mới xuất hiện, dẫn đến việc chỉ UI chọn `slot-01`; không sinh ra save hay tự động lưu. Hiện tượng này để sau này quyết định có lập task hay không bằng thao tác tay, hiện tại chỉ coi là ranh giới công cụ chạm tự động hóa.

### P9 phương án thiết kế giao diện độc lập màn hình ngang điện thoại (vòng 1)

- Kích thước thiết kế không phán đoán theo "model điện thoại", mà phán đoán theo tổ hợp viewport khả dụng thực tế: máy này là **expanded width (915dp) + compact height (412dp)**. Android chính thức xếp chiều cao `<480dp` vào chiều cao gọn màn hình ngang điện thoại, và nhắc rằng tổ hợp này không thể chỉ theo chiều rộng mà dùng bố cục 2 cột của tablet; dự án sau này sẽ dùng tổ hợp chiều rộng/cao của `visualViewport`, trạng thái fullscreen và khả năng chạm để cùng vào `mobile-landscape`, tiếp tục tách khỏi layout desktop.
- **Tầng cổng vào:** khi không fullscreen chỉ hiển thị một bìa khởi động nhẹ, giữ tiêu đề, gợi ý xoay ngang và nút chính duy nhất "Vào fullscreen trong trình duyệt"; nút bắt buộc phải nằm giữa dải khả kiến khoảng 155px hiện tại, sau khi bấm tái sử dụng `requestFullscreen()` hiện có, thành công mới hiển thị tiêu đề/game chính thức. Khi fail hiển thị tại chỗ hướng dẫn quyền trình duyệt/thao tác thủ công, không refresh, không đổi save, không ghi MVU.
- **Khung xương game chính:** dùng "sân khấu trung tâm + bàn thao tác hai bên + dải trần thuật dưới cùng" của game màn hình ngang trưởng thành. Bên trái cố định `136～160px` cho danh tính và 3 trạng thái quan trọng nhất; giữa là background, nhân vật và sự kiện hiện tại; bên phải `140～168px` chỉ đặt 3～5 thao tác tần suất cao; trên cùng `48～56px` đặt thời gian/địa điểm và điều khiển toàn cục; dải trần thuật dưới cùng khoảng `76～92px`, mở rộng khi có chính văn, thu hẹp khi không có. Đây là bố cục DOM/CSS chuyên dụng cho điện thoại, nhưng tái sử dụng trạng thái, component, ảnh và hàm sự kiện hiện có, không copy bộ logic nghiệp vụ thứ hai.
- **Khung xương dữ liệu/menu:** tái sử dụng "track điều hướng trái + vùng làm việc chi tiết phải" hiện có đã dùng được, mỗi mục điều hướng giữ 48px; ở chiều cao gọn mỗi lần chỉ nổi bật một task chi tiết, thao tác phụ vào vùng tràn cùng trang. Chủ tiệm, khách, nhân viên dùng chung vỏ ngoài, nhưng màu sắc và ưu tiên nội dung giữ khác biệt vàng nâu, tím vàng, xanh vàng tương ứng.
- **Trang chủ tiệm:** giữ vốn/thời gian/địa điểm bên trái và tổng quan/xếp ca/nhân viên/tuyển dụng bên phải hiện tại; trung tâm cố gắng nhường cho cảnh tắm suối nước nóng. Bộ chọn mascot đổi thành dải card nhân vật ngang, di động vô hiệu hóa `backdrop-filter` fullscreen, dùng gradient đặc màu tối; menu lớn giữ cấu trúc track trái/chi tiết.
- **Trang khách:** bên trái chỉ giữ số lần đến tiệm, vốn, chỉ định, bên phải chỉ giữ dịch vụ, chỉ định, quan hệ, danh bạ; nhân viên hiện tại/dịch vụ hiện tại làm nhãn nổi nhẹ ở sân khấu trung tâm, không còn cạnh tranh với 4 thao tác chính.
- **Trang nhân viên:** ảnh minh họa nhân vật tiếp tục chiếm sân khấu trung tâm; bên trái đánh giá/thể lực/vị trí/số dư, bên phải đến ca/khách tiếp theo/nghỉ/trưởng thành/thu chi; ngày, giờ, địa điểm đổi thành dải trạng thái trên cùng độc lập, tránh chồng lấp với đầu nhân vật; "task hiện tại + thao tác chính" ở dưới cùng gộp thành một dải lệnh có thể bấm.
- **Tiêu đề/chọn/setting/save:** trang tiêu đề giữ nguyên phân khu trái phải hiện tại; trang chế độ phân bổ lại khoảng trắng trung tâm; save giữ 4×3; setting nén control chọn hình tròn quá lớn thành vùng nóng hình chữ nhật gần với segmented control hệ thống hơn. Tất cả các trang giữ vị trí cố định cho thoát, fullscreen và quay lại, tránh mỗi trang phải học lại từ đầu.
- **Ánh xạ sản phẩm trưởng thành:** tham khảo "nội dung ưu tiên, điều khiển gộp nhóm, điều khiển tần suất thấp thu gọn" của duyệt/phát màn hình ngang YouTube, tham khảo "cảnh trung tâm và nhân vật ưu tiên, thao tác đặt ở rìa, menu phủ lên chứ không thay đổi cảnh nghiệp vụ" của game di động màn hình ngang trưởng thành như Genshin Impact, tham khảo "nội dung căn giữa, điều hướng tinh gọn, từ khám phá nhanh vào thao tác" mà bản di động Netflix 2026 công khai nhấn mạnh. Chỉ hấp thụ kiến trúc thông tin, không copy style thương hiệu.
- **Căn cứ quy chuẩn chính thức:** Android Window Size Classes (`https://developer.android.com/develop/adaptive-apps/guides/use-window-size-classes`), Layouts and navigation patterns (`https://developer.android.com/design/ui/mobile/guides/layout-and-content/layout-and-nav-patterns`), Accessibility (`https://developer.android.com/design/ui/mobile/guides/foundations/accessibility`), trợ giúp màn hình ngang YouTube (`https://support.google.com/youtube/answer/15994567`), trang App Store chính thức Genshin Impact (`https://apps.apple.com/us/app/genshin-impact/id1517783697`), giới thiệu trải nghiệm di động Netflix 2026 (`https://about.netflix.com/en/news/introducing-exciting-new-ways-to-find-and-enjoy-your-next-favorite-on-mobile`).

### P9 ma trận nghiệm thu vòng sau, dọn dẹp và trạng thái phát hành

- Ưu tiên triển khai vòng sau: `P0-1 tầng khởi động không fullscreen → P0-2 hợp thành mask mascot → sắp xếp lại chọn chế độ → thống nhất mật độ thông tin 3 chế độ chơi → hoàn thiện bảng màu và hiệu ứng động`. Mỗi bước làm máy thật Android trước, rồi bổ sung không hồi quy desktop; không viết lại toàn bộ trang một lần.
- Viewport mục tiêu: ít nhất bao phủ Chrome Android `915×412`, `800×360`, `740×360`, trạng thái nhúng sau khi mở rộng thanh trình duyệt, Fullscreen API chuẩn thành công/bị từ chối, xoay ngang→dọc→lại ngang; kiểm tra thay đổi `visualViewport`, bàn phím/thanh địa chỉ, gợi ý giáo dục fullscreen lần đầu của Chrome và vùng nóng chạm 48px.
- Hồi quy 3 chế độ chơi: mỗi chế độ chơi kiểm tra save mới/cũ, màn hình chính/menu, quay lại/fullscreen/xoay, lưu/tải qua lại và vào lặp lại; sau khi sửa chính thức chạy lại hồi quy có sẵn của MVU, world book, AIRP, queue AI đơn, tự động lưu, quyết toán kinh tế. Vòng audit hình ảnh thuần túy này không kích hoạt các chuỗi nghiệp vụ đó, nên không giả vờ đã nghiệm thu nghiệp vụ sâu.
- Vòng này chỉ bổ sung file kỹ thuật vào `src/汤泉/二期体验升级清单.md`; frontend chính thức, character card, world book, Maya và bundle đều không sửa. Thêm công cụ ổ D `D:\AndroidDev\Tools\cdp-touch.mjs` cho QA Android và cập nhật `D:\AndroidDev\README.md`, `node --check` pass.
- ESLint/kiểm tra kiểu/tự động hóa/build chính thức: vòng này không sửa mã nguồn chính thức, nên không chạy lại; dùng baseline build P8 trước khi vào audit, không diễn giải "không có lỗi biên dịch mới" thành xác minh chức năng.
- Kết quả dọn dẹp: đã khôi phục Vue UI memory về trang tiêu đề/panel chế độ trước audit, runtime xác nhận lại là không có slot hoạt động/chưa chọn chế độ chơi; 12 slot vẫn rỗng, tự động lưu `dirty=false`. `window.__TQ_P9_ANDROID_AUDIT__` duy nhất, style chẩn đoán backdrop inline tạm, toàn bộ screenshot `p9-*` trên ổ D và `/sdcard/p9-*.png` trong emulator đã xóa; 8879 không còn listener, không tạo entry world book, ghi MVU, yêu cầu AI, thay đổi kinh tế hay save test. Kết nối emulator và debug 8000/9223 được giữ lại cho vòng sau tiếp tục dùng.
- Chưa giải quyết: 2 vấn đề P0 nêu trên chưa sửa mã nguồn chính thức; các trang nghiệp vụ sâu của 3 chế độ chơi chỉ audit màn hình chính và menu cấp 1, chưa vượt quyền kích hoạt thao tác cần save/AI/world book/kinh tế ở trạng thái chạy rỗng.
- Hướng thẩm mỹ cần người dùng quyết định: lựa chọn hàng đầu là **"bàn kinh doanh điện ảnh"** (giữ background suối nước nóng phong cách Nhật hiện có, nhân vật/cảnh ở trung tâm, bàn chức năng bán trong suốt hai bên, điểm màu vàng/xanh độ bão hòa thấp); lựa chọn dự phòng là "HUD game thủ mạnh hơn" (icon và trạng thái nổi bật hơn) hoặc "App khách sạn cao cấp" (card chữ sạch hơn, sự hiện diện nhân vật thấp hơn). Trước khi người dùng xác nhận hướng thì không bắt đầu viết lại hình ảnh trên diện rộng.
- Trạng thái phát hành: không có thao tác commit, push, tag phiên bản, GitHub, CDN hay thông báo; vòng audit này không thay đổi trạng thái "chưa phát hành" hiện có.

## Task mới thêm vòng này: P10 triển khai chính thức vòng đầu và nghiệm thu máy thật màn hình ngang điện thoại
