# Danh sách nâng cấp bảo trì Hoa Chưa Nở

> File này là TODO duy nhất có hiệu lực và file tiếp nối ngữ cảnh cho giai đoạn nâng cấp phiên bản chính thức, bảo trì và sửa lỗi (BUG).
> `项目进度清单.md` (Danh sách tiến độ dự án) chỉ dùng làm lịch sử phiên bản cơ sở, không xóa, không viết lại, không chạy lại các task cũ đã hoàn thành trong đó.

## Giai đoạn hiện tại

- Giai đoạn: hồi quy cuối cùng P4 đã hoàn thành
- Ưu tiên hiện tại: giữ baseline máy thật trống rỗng cuối cùng, chờ người dùng xác nhận cuối cùng; không push GitHub/CDN
- Trạng thái hiện tại: P0, P1, P2, P3, P4 và bảo trì fullscreen gốc trình duyệt đều đã hoàn thành. Ma trận đầy đủ của P4, sửa BUG phát hiện trên máy thật, tự động hóa cuối cùng, ESLint, build hoàn chỉnh, xuất ghi nhận vấn đề và dọn dẹp QA chính xác đều đã pass; Chrome ngoài hiện tại là trang tiêu đề, không có save, không có tầng QA, MVU/định dạng xuất chuyên dụng đều đã xóa sạch, service inject cục bộ đã dừng.
- Ranh giới công việc: không sửa HTML preview; không tái cấu trúc UI ổn định; không sửa đổi chính văn world book hay từ ngữ prompt; không push GitHub/CDN.

## Việc cần làm

### Chuẩn bị và baseline

- [x] Đã audit đầy đủ cổng vận hành chính thức, queue AI, sinh nội dung, tầng (floor), MVU, world book động và kết nối save của `src/汤泉/脚本/零层美化/`.
- [x] Đã audit cấu trúc entry, entry động và tham chiếu quy tắc biến của `src/汤泉/index.yaml`.
- [x] Đã audit cấu trúc preset chính thức và các entry liên quan bật trong vòng này của `src/明月秋青Maya/`; chỉ đọc chính văn prompt, không sửa từ ngữ.
- [x] Đã ghi lại các thay đổi có sẵn liên quan đến task này trong Git working tree hiện tại, tránh ghi đè file của người dùng.

### P0: nối dây input và nhân vật

- [x] Thêm log chẩn đoán có thể export cho AIRP thường, tin nhắn danh bạ và reroll: cả 3 chuỗi máy thật đều đã hiển thị đầy đủ giá trị ô nhập, `generate.user_input`, lịch sử cuối cùng, sceneId, ID người tham gia và phản hồi gốc.
- [x] Xây dựng cấu trúc danh tính hiện trường (scene) đơn nhất: `sceneId / mode / kind / speakerId / participantIds / locationId / service/project/assignmentId`.
- [x] Xây dựng bộ dựng lịch sử chat dùng chung cho gửi thường và reroll.
- [x] Đảm bảo input hiện tại chỉ được thêm một lần trong request cuối cùng, không tạo tầng người dùng trước khi sinh.
- [x] Giữ toàn bộ người tham gia và đối thoại của hiện trường liên tục hiện tại; khi chuyển sang hiện trường không liên quan thì cách ly lịch sử từng câu.
- [x] Bao phủ rủi ro tương tự cho tin nhắn danh bạ.
- [x] Việc sửa tầng, xóa tầng/rollback và rebuild lịch sử hiện trường sau khi load save đã pass nghiệm thu bằng code, dữ liệu thuần và Chrome ngoài thực tế.
- [x] Đã sắp xếp bàn giao sửa preset Claude: chỉ liệt kê vấn đề, file, mục tiêu và tiêu chí nghiệm thu, không viết mẫu prompt hay chính văn.
- [x] 3 chỗ phụ thuộc `{{lastUserMessage}}` của Claude đã được sửa; phúc tra ngữ nghĩa vòng đầu, A/B/C, thường/reroll và danh bạ A/B đều đã pass, P0 hoàn thành.

### Hạng mục bảo trì độc lập: fullscreen gốc trình duyệt

- [x] Dùng Fullscreen API của trình duyệt để container iframe ổn định vào fullscreen, không giả lập fullscreen bằng CSS, không thay đổi trạng thái chơi/save.
- [x] Trang tiêu đề và trang chơi ở góc trên phải đều có nút SVG thường trực: 4 góc ngoài biểu thị vào fullscreen, 4 góc trong biểu thị thoát fullscreen.
- [x] ESC chỉ thoát fullscreen trình duyệt và ở lại trang, chế độ chơi và hiện trường hiện tại; nút góc trên phải có thể thực hiện thao tác thoát tương tự.
- [x] Lắng nghe `fullscreenchange` để đồng bộ trạng thái thật; khi request fail vẫn giữ game hiện tại và đưa ra gợi ý nhẹ.
- [x] Đã hoàn thành ESLint có định hướng, build và xác minh máy thật Chrome ngoài.

### P1: AI sinh khách cho tuyến nhân viên

- [x] Đã thiết kế và triển khai cấu trúc khách ổn định tương thích save cũ cùng migration/sửa lỗi.
- [x] Yêu cầu khách mỗi ngày kết nối vào queue AI đơn có sẵn; cổng attempted trong ngày chống trùng, hoàn thành thì ghi ngay kết quả hoặc fallback vào save hoạt động.
- [x] Đã triển khai phân tích XML nghiêm ngặt và xác minh trường/ID/ngân sách/dự án/khung giờ/thay đổi giới tính/chỉ định xuyên ngày ở frontend; khi fail, phản hồi rỗng hoặc sai định dạng sẽ dùng fallback cục bộ có nhiều giới tính khác nhau.
- [x] Xếp ca, hiện trường, chỉ định, MVU và world book đã thống nhất dùng cùng guestId; chỉ định xuyên ngày giữ hồ sơ đầy đủ theo guestId.
- [x] Giới hạn của các nhân vật như Bạch Tinh vẫn giữ nguyên đường nối cũ; assertion có định hướng xác nhận Bạch Tinh vẫn không khả dụng cho người dùng không phải nữ, khả dụng cho người dùng nữ, P1 không sửa danh mục nhân vật hay gameplay khách.
- [x] Sau khi Claude cập nhật preset output hàng ngày của nhân viên và sửa từ ngữ xung đột trong phần inject frontend, đường thành công AI, cổng cùng ngày, load save và hồi quy máy thật toàn chuỗi guestId đều pass, P1 hoàn thành.

### P2: báo cáo kinh doanh ngày của chủ tiệm

- [x] Đã đóng băng và quyết toán xác định ở frontend cho sự thật trong ngày.
- [x] Sự thật chỉ-đọc từ quyết toán được sinh thành bản tóm tắt kinh doanh qua queue AI đơn, phân tích nghiêm ngặt rồi ghi vào bản ghi quyết toán ngày hôm đó.
- [x] Khi AI fail dùng báo cáo cục bộ, quyết toán không fail, không bị AI sửa số liệu.
- [x] Sau khi báo cáo hoàn thành mới qua ngày và tự động lưu.
- [x] Nếu cần thêm output chuyên dụng, chỉ sắp xếp bàn giao cho Claude, không viết chính văn prompt.

### P3: tự động lưu áp lực thấp

- [x] Xây dựng cờ bẩn (dirty flag), kích hoạt rảnh rỗi/định kỳ, các mốc bắt buộc qua ngày/về tiêu đề/chuyển save.
- [x] Xây dựng gộp yêu cầu lưu và loại trừ lẫn nhau đơn task.
- [x] Cấm tự động lưu khi AI, queue, MVU, world book, thao tác tầng, loading, popup xác nhận đang hoạt động.
- [x] Ghi lại thời gian, số phân mảnh và nguyên nhân fail, lưu fail không phá hỏng save hiện tại.
- [x] Đã xác minh áp lực chat dài và world book lớn.

### P4: hồi quy cuối cùng

- [x] Hồi quy toàn diện 3 chế độ chơi, hướng dẫn, qua ngày, save, load, hiện trường nhiều người, thao tác tầng, world book động, MVU, lỗi API, migration save cũ, áp lực tự động lưu.
- [x] Chỉ dùng Chrome ngoài đã mở sẵn của người dùng tại `http://127.0.0.1:8000/` và chrome-devtools MCP để nghiệm thu máy thật.
- [x] Kiểm tra listener thời gian thực của Tavern Helper; sau khi sửa code thì cập nhật theo đường build/inject cục bộ chính thức.
- [x] Đã dọn tầng QA, save QA, trạng thái test, entry động và hiện trường tương tác, không xóa save thật của người dùng.
- [x] Đã chạy ESLint liên quan và `pnpm build:dev`.

## Việc đã hoàn thành

- [x] 2026-07-16: P4 chốt cuối cùng hoàn thành. `log.exportJson()` chính thức đã được export qua Chrome ngoài tới `C:\Users\三明月\Desktop\未开之花-P4-问题记录-20260716.json`, 6359 bytes, format/version/release/context/SHA256 bundle/lastMessageId/ma trận hồi quy đều đã xác minh pass. 10 hạng mục tự động hóa cuối cùng lại pass toàn bộ: phân tích/lịch sử hiện trường, sắp xếp hàng ngày 1000 vòng, migration khách/save cũ, báo cáo/kinh doanh chuyên sâu, nhân viên 365 ngày, khách 492 ngày, tự động lưu 1000+100 gộp, world book rollback 3 giai đoạn và áp lực 8 phân mảnh. Mã nguồn và test liên quan ESLint 0 error/0 warning; `boss_deep_qa.cjs` thuần CommonJS chỉ tắt quy tắc `no-require-imports` không áp dụng qua CLI rồi pass, không sửa logic test. `pnpm build:dev` cuối cùng toàn bộ webpack thành công, bundle Hoa Chưa Nở 8,064,544 bytes, SHA256 `B294CFEF72F8DD109FBBDEC866635065508AA331D28E15F165C8ED9DD4825F29`; inject lại trên trình duyệt số byte/hash hoàn toàn giống nhau.
- [x] 2026-07-16: dọn dẹp chính xác QA của P4 hoàn thành. Trước khi xóa đã đối chiếu từng save, tên người dùng của `slot-10/11/12` đều bắt đầu bằng `P4_QA_20260716_`, đối chiếu từng tầng 1..12 thì 6 tầng người dùng đều mang cùng ký hiệu và tất cả tầng trợ lý đều có scene trace tương ứng; sau đó chỉ xóa 3 save QA và tầng 1..12, giữ tầng gốc 0 `<三明月>`. Cuối cùng `slots=[] / activeSlotId='' / activeMode=未选择`, world book save 0 entry; 5 entry động mỗi loại duy nhất, tắt và chính văn rỗng; MVU tầng 0 có `initialized_lorebooks` rỗng, stat chỉ có thời gian hiện tại, `用户.所在地=未开之花汤泉` và `互动现场={}`; định dạng xuất chuyên dụng là none, inject lỗi toàn cục đã xóa, trang tiêu đề có 1 iframe game, Loading tắt, "cho phép giám sát" đã tick, log 0 error/0 warning. Service cục bộ 8879 PID 36212 đã dừng và không còn listener, chưa xóa save thật của người dùng, chưa push GitHub/CDN.
- [x] 2026-07-16: P4 đã pass và khôi phục hoàn chỉnh việc inject lỗi thật cho các trường hợp API phản hồi rỗng/sai định dạng/timeout/fail. Mỗi loại lỗi đều thay thế tạm `generate` trong realm iframe script Tavern Helper rồi inject lại bundle độc lập, sau khi test xong lập tức rollback và thêm tầng 13/14 mới hoặc xác nhận không tạo tầng nào. Phản hồi rỗng chỉ gọi một lần và rơi vào `<content>对方暂时没有继续回应。</content>`, tầng user/assistant 1/1 tạo bình thường; định dạng lỗi không rỗng không có `<content>` ghi nhận `formatIssues=[缺少 content 标签]` và hiển thị text không phản hồi cục bộ, không crash; giả lập `TimeoutError` bị từ chối 80ms và exception thường đều hiển thị "hiện trường tương tác thất bại, vui lòng thử lại sau", không tạo tầng, nút gửi khôi phục, queue AI `pending=0`. Cuối cùng đã khôi phục chính xác `generate` của helper về hàm gốc và inject lại, `lastMessageId=12` hiện tại, Loading tắt, log instance mới 0 error. Lỗi cấu trúc hóa nghiêm ngặt và fallback cục bộ của P1/P2 đã được bao phủ bởi 1000 vòng sắp xếp hàng ngày tự động hóa vòng này, 11 nhóm ngoại lệ báo cáo và bằng chứng từ chối nghiêm ngặt trên Chrome ngoài có sẵn.
- [x] 2026-07-16: P4 save/reload/unmount world book động và MVU hiện trường tương tác đã pass máy thật. Trong scene 1 người của Cầm Âm, `area.current` và `character.current-employee` được bật và ghi lần lượt 60/100 ký tự sự thật, MVU nạp `互动现场/当前员工/当前关系`; khi scene vẫn mở đã ép lưu hoàn chỉnh (4 phân mảnh, 106263 bytes, checksum `95cb84e2`) rồi unmount/inject lại ngay, trang, scene, 2 chính văn động và toàn bộ block MVU khôi phục nguyên trạng. Sau khi rời hiện trường 5 entry động đều `enabled=false/contentLength=0`, MVU chỉ giữ thời gian hiện tại, người dùng và `互动现场={}` rỗng, định dạng xuất chuyên dụng khôi phục none, chứng minh toàn chuỗi nạp, cập nhật AI, lưu, khôi phục khi load save, unmount và xóa sạch đều pass.
- [x] 2026-07-16: P4 hồi quy máy thật AI/tầng/danh bạ/hiện trường nhiều người đã pass. Input `STORY_A` của story nhân viên có lịch sử cuối cùng rỗng, input hiện tại chỉ append 1 lần, phân tích nghiêm ngặt `<content>` AIRP và tầng đọc chính văn đều pass; reroll dùng cùng sceneId/cùng input/cùng lịch sử trước đó, sau khi xác nhận rollback thì xóa theo cặp tầng 1/2 về tầng 0. Nhóm user thứ hai bị chỉnh sửa từ bên ngoài từ `EDIT_ORIGINAL` thành `EDITED_FLOOR`, chẩn đoán reroll đọc đúng text sau khi sửa và lịch sử vẫn rỗng; giả lập sự kiện `MESSAGE_DELETED` thật của Tavern xóa tầng 2/1, trang khôi phục hiển thị vào lại hiện trường, nút sinh bị vô hiệu hóa, chứng minh sửa/xóa tầng rebuild theo thời gian thực. Dialogue nhân viên-chủ tiệm bật Galgame bình thường và phân tích 5 trang. Khách atri yêu cầu liên hệ thành công, MVU chỉ cập nhật thiện cảm/số lần tương tác/trạng thái liên hệ theo phản hồi, sau khi rời hiện trường danh bạ xuất hiện; tin nhắn danh bạ online dùng scene độc lập và unmount entry danh bạ sau phản hồi, khôi phục trạng thái offline. Để test riêng hiện trường 3 người, đã inject 2 tiền đề test danh bạ đã thêm là Cầm Âm/Nếp Nếp trong save QA khách chỉ chứa `P4_QA_20260716` (không giả làm kết quả mở khóa tự nhiên), sau đó mời atri/Cầm Âm/Nếp Nếp: chính văn nhân viên hiện tại động chứa đồng thời 3 hồ sơ, MVU互动现场 chứa quan hệ 3 người, chẩn đoán sinh participantIds là ID ổn định của 3 người; vòng 2 cùng scene có lịch sử chính xác chứa 2 mục user/assistant của vòng 1 và input hiện tại chỉ append 1 lần. Sau khi rời hiện trường 3 người vào scene 1 người của Cầm Âm, lịch sử trước đó là 0, participantIds chỉ có Cầm Âm, phản hồi chỉ xử lý bận/rảnh công việc, không mang theo đối thoại từng câu của 3 người.
- [x] 2026-07-16: BUG bịa đặt lịch xếp ca trong đối thoại nhân viên-chủ tiệm mới phát hiện của P4 đã sửa và đóng trên máy thật. Nguyên nhân gốc là `openWaiterBossTalk()` chỉ cung cấp cho model khu vực hiện tại và "đang trò chuyện", không cung cấp `waiterState.assignments`, khi bị hỏi về lịch xếp ca model chỉ có thể tự bịa. Nay đã thêm hàm thuần serialize ID ổn định, thời gian, guestId/xưng hô/giới tính/chủng tộc, nguồn, dự án, khu vực, thời lượng và trạng thái của mỗi assignment thành sự thật chỉ-đọc, đồng thời ghi vào chính văn `area.current` tạm và MVU `互动现场.当日接待安排`; không sửa chính văn prompt. File đã sửa: `ui/app/waiterGame.ts`, `ui/app/App.vue`, `scripts/test_tangquan_waiter_guests.ts`. Test có định hướng mới thêm assertion khớp từng trường 3 sự thật mới với hồ sơ/lịch xếp ca và pass, 3 file ESLint 0 error, `pnpm build:dev` toàn bộ webpack thành công; bundle mới 8,064,544 bytes, SHA256 `B294CFEF72F8DD109FBBDEC866635065508AA331D28E15F165C8ED9DD4825F29` đã inject lại và hash trình duyệt khớp. Máy thật entry động và MVU đều xuất hiện 3 sự thật thật; với cùng câu hỏi, model thuật lại chính xác `18:00 粗犷商贩·入浴休憩 / 19:40 米娅·理疗按摩 / 21:50 泥石行者·入浴休憩`, giới tính, chủng tộc, khu vực, thời lượng và trạng thái đều đúng và không thêm lịch mới.
- [x] 2026-07-16: ma trận máy thật Chrome ngoài 3 chế độ chơi của P4 đã pass. Chủ tiệm `slot-10` hoàn thành 13/13 hướng dẫn, xác nhận xếp ca, báo cáo kinh doanh AI, ngày kinh doanh 1→2, vốn 128400→126544, qua ngày 00:00 và lưu, `inspectSlot.ok=true`; khách `slot-11` hoàn thành 11/11 hướng dẫn, chỉ định atri, đặt lịch/bắt đầu/rời/kết thúc入浴休憩, vốn 30000→28820, AI sắp xếp ngày hôm sau, số lần đến tiệm 1→2, qua ngày 09:00 và lưu; nhân viên `slot-12` hoàn thành 9/9 hướng dẫn, AI sinh 3 khách ngày đầu là nam người/nữ tiên nữ/vô tính slime với `guestGeneration.source=ai` và cả 3 assignment/hiện trường/MVU đều dùng cùng guestId, hoàn thành 3/3 tiếp khách và quyết toán tan ca (thu 720, chi 180, lãi ròng 540), ngày hôm sau AI lại sinh nam người/nữ bán thú/vô tính tinh linh nguyên tố và lưu. Sau đó qua UI save chính thức lần lượt load chủ tiệm, khách, nhân viên, cả 3 đều khôi phục đúng ngày, số tiền, trạng thái hoàn thành hướng dẫn và chế độ; mỗi lần chuyển đều tự động lưu save cũ trước, Loading cuối cùng tắt.
- [x] 2026-07-16: hồi quy toàn phần tự động hóa của P4 đã pass. Phân tích phản hồi AI, 10 nhóm lịch sử hiện trường P0, sắp xếp hàng ngày version 2/1000 vòng phân tích nghiêm ngặt/queue đơn preset P1, đa giới tính khách nhân viên/world book và MVU cùng ID/migration xác định save cũ, báo cáo P2 bình thường/nghỉ bán/11 nhóm ngoại lệ từ chối/migration save cũ/ma trận kinh doanh 12×90 ngày/áp lực 160 ngày, tự động lưu P3 7 nhóm và gộp 1000+100, rollback world book 3 giai đoạn/tối đa đồng thời 1/8 phân mảnh 495120 bytes đều pass. Áp lực nhân viên 365 ngày/1440 lần phục vụ/0 lần fail khởi động/hạng S; áp lực khách 3957 lần thao tác thành công/492 ngày/20 danh bạ, đều không thoát bất thường. Repo hiện tại chưa cài `tsx` executable, 4 test TypeScript đổi dùng `ts-node --transpile-only` đã cài sẵn cùng cách phân giải CommonJS/Node, logic test không đổi và đều pass.
- [x] 2026-07-16: khôi phục baseline Chrome ngoài và inject lại bundle cuối cùng của P4 hoàn thành. Service CORS cục bộ 8879 PID 36212 đã khởi động; qua iframe script `【Sanmingyue】预设工具` đã bật để inject, trình duyệt thực sự lấy về 8,053,652 bytes, SHA256 `9B4DB8619B911A355E4EC4C5573D0DBA1734928528937F33714567B5672160E2`, giống hệt từng byte với bản cục bộ. Sau khi inject chỉ có 1 iframe game, trang tiêu đề khả kiến; `slots=[] / activeSlotId='' / activeMode=未选择`, Loading tắt, "cho phép giám sát" đã tick; world book save 0 entry, 5 entry động mỗi loại duy nhất, tắt và chính văn rỗng; MVU chỉ có thời gian hiện tại/người dùng/互动现场 rỗng, chat chỉ giữ tầng gốc 0. Log Hoa Chưa Nở 16 dòng, 0 error, 0 warning. Không tạo hay xóa save/tầng nào.
- [x] 2026-07-16: P4 khởi động chính thức và đọc lại danh sách bảo trì duy nhất. Đã xác nhận cổng vào chính xác là bundle P3 cuối cùng (8,053,652 bytes, SHA256 `9B4DB8619B911A355E4EC4C5573D0DBA1734928528937F33714567B5672160E2`) và baseline trang tiêu đề trống rỗng đã dọn dẹp; hạng mục này chỉ khôi phục ngữ cảnh và định thứ tự hồi quy, chưa tạo, sửa hay xóa bất kỳ save/tầng/entry động nào.
- [x] 2026-07-16: dọn dẹp và chốt QA cuối cùng của P3 hoàn thành. Chỉ xóa `slot-10/11/12`, tầng 1..122 và dữ liệu áp lực mang ký hiệu duy nhất được tạo trong vòng này; giữ tầng gốc 0 `<三明月>`, không xóa bất kỳ save thật nào của người dùng. Cuối cùng `slots=[] / activeSlotId='' / activeMode=未选择`, entry world book save chính thức và staging đều là 0; 5 entry động mỗi loại duy nhất, `enabled=false`, chính văn dài 0; MVU chỉ còn thời gian hiện tại, người dùng cơ bản và `互动现场={}` rỗng, block khởi tạo rỗng; preset `format=none`, 3 output chuyên dụng đều tắt; trang tiêu đề có 1 iframe game, Loading tắt, "cho phép giám sát" bình thường. Service inject CORS cục bộ 8879 đã dừng, không còn listener.
- [x] 2026-07-16: các cổng cuối cùng của P3 đều đã pass. File nguồn liên quan Prettier/ESLint 0 error/0 warning; tự động lưu 7 nhóm (bao gồm 1000 lần cờ bẩn+100 lần gộp yêu cầu thành 1) và test rollback fail 3 giai đoạn/tối đa đồng thời 1/8 phân mảnh 495120 bytes world book lại pass trên mã cuối cùng; `pnpm build:dev` cuối cùng toàn bộ mục tiêu webpack thành công. Chưa push GitHub/CDN.
- [x] 2026-07-16: áp lực chat dài/world book lớn Chrome ngoài của P3 đã pass. Đã thêm tạm 120 tầng mang ký hiệu duy nhất `P3_LONG_CHAT_QA_001..120`, tổng khoảng 482400 ký tự; ép lưu im lặng mất 1073ms đồng hồ tường (controller 1072ms), sinh ra 46 phân mảnh chính thức, 1,019,276 bytes, checksum `b1848021`, trong lúc đó Loading luôn tắt. Sau đó `readSlotData + inspectSlot` chính thức đọc lại: `ok=true / repairable=false / messageCount=123`, số lượng khai báo và thực tế snapshot chat đều là 123, 120 ký hiệu QA liên tục đầu-cuối, checksum snapshot `e2033c27`. Chứng minh việc bắt chat dài, ghi phân mảnh triệu byte và đọc lại đầy đủ đều không bị kẹt hay mất dữ liệu rõ rệt.
- [x] 2026-07-16: sửa lỗi listener hoạt động tài liệu game của P3 đã build và pass máy thật. 3 file liên quan App/Loading/world book đều pass Prettier, ESLint 0 error/0 warning, tự động lưu 7 nhóm thời gian tiếp tục pass, `pnpm build:dev` toàn bộ thành công. Gói inject trình duyệt cuối cùng 8,053,652 bytes, SHA256 `9B4DB8619B911A355E4EC4C5573D0DBA1734928528937F33714567B5672160E2` khớp với bản cục bộ; sau khi reload 1 iframe game, save QA 3 chế độ chơi và chính văn AI khách đều khôi phục hoàn chỉnh. Sau khi gửi pointerdown tới `ownerDocument` của iframe game, chẩn đoán `lastActivityAt` lập tức cập nhật từ `1784141458129` thành `1784141465752`, chứng minh thao tác màn hình thật hiện đã đúng cách trì hoãn fallback rảnh 3 phút.
- [x] 2026-07-16: đã phát hiện và sửa lỗi lệch realm của listener hoạt động rảnh trên máy thật của P3. Nguyên nhân gốc là module App chạy trong iframe script Tavern Helper, còn Vue game được mount vào iframe "Hoa Chưa Nở" độc lập khác; `document.addEventListener(pointerdown/keydown/input)` gốc lắng nghe tài liệu iframe script, click thật trong màn hình game không refresh `lastActivityAt`, dẫn đến khi chạy quá 3 phút dù người chơi vẫn đang thao tác vẫn có thể kích hoạt lưu idle. Nay đổi thành lấy tài liệu game thực tế từ `appRootElement.ownerDocument` và giữ tham chiếu binding, khi unmount gỡ listener khỏi cùng tài liệu đó. File đã sửa: `ui/app/App.vue`. Chờ rebuild inject lại rồi dùng pointerdown màn hình game để phúc tra.
- [x] 2026-07-16: ép lưu trước khi chuyển save của P3 đã pass máy thật. Khi `slot-11` đang hoạt động đã đánh dấu nhân tạo "thay đổi save cũ trước khi chuyển save", sau đó load `slot-10` từ panel save chính thức; sau khi xác nhận đã gộp lý do này với "trước khi chuyển sang save slot-10" và ghi lại vào save nhân viên cũ, 721ms, checksum `5b61a53d`, hoàn thành mới chuyển runtime thành `activeSlotId=slot-10 / activeMode=游客`. Trạng thái 09:00 ngày hôm sau của khách được load hoàn chỉnh, controller clean, Loading tắt, chứng minh save mục tiêu không ghi đè save cũ chưa lưu.
- [x] 2026-07-16: save mới của nhân viên, fallback rảnh và qua ngày của P3 đã pass máy thật. Khi khách mới/lịch xếp ca AI, MVU và khởi tạo trang của `slot-11` kết thúc, lần ép lưu đầu tiên trùng đúng lúc Loading kết thúc nên bị cổng trì hoãn 15 giây, sau đó tự động bổ sung ghi một lần và khôi phục clean. Trong lúc hoàn thành 3/3 tiếp khách vừa vặn qua ngưỡng rảnh 3 phút, nhiều cờ bẩn `服务员到岗/开始接待/结束接待` bị gộp bởi `trigger=idle` thành một lần ghi, 783ms, checksum `c0e80b5e`, không làm gián đoạn việc chơi. Sau khi tan ca mới quyết toán lương ngày/chi phí sinh hoạt, đẩy ngày mới, sinh khách AI và lịch xếp ca ngày hôm sau và đồng bộ MVU, cuối cùng mới gộp ép ghi một lần "服务员下班结算/服务员跨日流程完成", 653ms, 10594 bytes, checksum `eca02e5f`; Loading tắt, ca và danh tính chỉ định xuyên ngày hôm sau vẫn hiển thị.
- [x] 2026-07-16: save mới và qua ngày của khách trong P3 đã pass máy thật. `slot-10` ngày đầu ghi save cơ bản trước, rồi queue AI đơn sinh lịch ngày đầu; sau khi AI/MVU/khởi tạo trang đều hoàn thành mới gộp lưu im lặng "quy trình khởi tạo save mới hoàn thành/khởi tạo save mới hoàn thành", 1 phân mảnh, 19282 bytes. Sau đó đẩy từ "hành trình→nghỉ ngơi" sang 09:00 ngày hôm sau: trong lúc sinh lịch ngày hôm sau chưa lưu trước, sau khi chính văn lịch và MVU cập nhật xong mới gộp ghi "nghỉ sang ngày hôm sau/quy trình qua ngày của khách hoàn thành", mất 804ms, 19679 bytes, checksum `b8831c32`, controller clean, Loading tắt, trang hiển thị bản ghi ngày mới.
- [x] 2026-07-16: sửa lỗi đồng thời Loading của P3 đã rebuild và inject lại vào Chrome ngoài. Trình duyệt thực sự lấy về 8,051,064 bytes, SHA256 `F4D388EDD38286C886ABEAF047DDB1707378891DEF1840975AE56632A9FDECAC`, khớp từng byte với bản cục bộ; sau khi reload vẫn chỉ có 1 iframe game, save chủ tiệm `slot-12` và trạng thái 00:00 sau khi qua ngày khôi phục hoàn chỉnh. Máy thật đồng thời phát 3 lần đọc danh sách save và 2 lần đối chiếu trạng thái chạy: đều trả về cùng một save, mask ngay khoảnh khắc task kết thúc là 100%, sau 350ms tự động thành `visible=false`, không còn bị treo. BUG này đã đóng trên máy thật.
- [x] 2026-07-16: dư thừa Loading của P3 tiếp tục được sửa ở cấp controller. Ngoài việc bỏ `update()` vượt quyền của danh sách tầng dưới, `loadingOverlay.run()` cũng đổi từ "coi task đồng thời là cấp lồng nhau" sang đếm theo task hoạt động: task đầu tiên hiển thị mask, tất cả task song song/lồng nhau kết thúc mới thống nhất sắp xếp ẩn; bất kỳ task nào fail sẽ giữ trạng thái fail 1600ms, tất cả thành công thì 180ms sau ẩn. Nhờ vậy task đồng thời hoàn thành sau sẽ không còn xóa mất timer ẩn duy nhất. File đã sửa: `ui/loadingOverlay.ts`, `save/worldbookSave.ts`.
- [x] 2026-07-16: các cổng cục bộ sau khi sửa quyền sở hữu Loading đã pass: `worldbookSave.ts` và test tương ứng ESLint 0 error/0 warning; test rollback fail/đồng thời/áp lực 8 phân mảnh world book tiếp tục pass; test thời gian tự động lưu 7 nhóm và áp lực 1000 lần đánh dấu bẩn tiếp tục pass; `pnpm build:dev` toàn bộ mục tiêu webpack biên dịch thành công. Bước tiếp theo là inject lại bản build này, phúc tra trên cùng save QA giữ lại của Chrome ngoài xem Loading có tự động tắt sau khi qua ngày không.
- [x] 2026-07-16: nghiệm thu máy thật qua ngày của chủ tiệm P3 đã phát hiện và sửa dư thừa Loading. Nguyên nhân gốc là `worldbookSave.listSlots()` tầng dưới tự gọi `loading.update()`; khi 2 `loading.run()` tầng trên chạy đồng thời xen kẽ, task lồng bắt đầu sau sẽ xóa timer ẩn trì hoãn của task gốc, nhưng vì bản thân nó không phải root nên không sắp xếp ẩn nữa, dẫn đến `visible=true` tồn đọng vĩnh viễn và tiếp tục chặn tự động lưu sau đó. Đã xóa cập nhật tiến độ không có quyền sở hữu ở tầng dưới, tiến độ danh sách vẫn do task Loading cấp gốc của `saveController.listSlots()/reconcileRuntime()` chịu trách nhiệm; refresh im lặng không còn tự ý hiển thị mask. File đã sửa: `save/worldbookSave.ts`. Chờ rebuild và inject lại rồi phúc tra.
- [x] 2026-07-16: cổng popup xác nhận và retry trì hoãn của P3 đã pass máy thật. Khi panel save tiêu đề mở popup xác nhận "xóa save", ép tự động lưu lập tức trả về `status=deferred`, lý do chặn là "popup xác nhận vẫn đang mở", controller giữ `dirty=true / saving=false / requestPending=true / forcePending=true`, không ghi. Sau khi hủy popup, yêu cầu vẫn tuân theo backoff cổng 15 giây chứ không chạy trước; đến giờ gộp 3 lý do và chỉ ghi một lần, 955ms sau khôi phục clean, checksum `d19ca8ad`, kiểm tra chính thức vẫn `ok=true`. Save QA không bị xóa.
- [x] 2026-07-16: ép lưu trước khi về tiêu đề của P3 đã pass máy thật. Khi save chủ tiệm đang ở trang game, đánh dấu thay đổi chưa lưu trước, rồi kích hoạt "về tiêu đề" góc trên phải; quy trình tự động gộp "P3 QA popup xác nhận chặn" và "trước khi về tiêu đề" thành một lần ghi `trigger=force`, hoàn thành trong 923ms, checksum cập nhật từ `797e7f2d` thành `daf61d99`, sau đó mới về trang tiêu đề hoàn chỉnh. Sau khi về, controller `dirty=false / saving=false`, Loading tắt, slot hoạt động vẫn giữ là `slot-12` để tiếp tục chơi, không xảy ra mất save hay nhảy trang giữa chừng.
- [x] 2026-07-16: gộp nhiều lý do và ép lưu chẩn đoán của P3 đã pass máy thật. Sau khi đánh dấu liên tục `P3 QA 合并原因 A/B` rồi yêu cầu ép lưu một lần, kết quả cuối cùng chỉ sinh một dòng `status=saved / trigger=force`, `reasons` chứa đồng thời A, B và lý do ép; mất 989ms, vẫn là 1 phân mảnh, 9769 bytes, checksum mới `797e7f2d`. Sau đó `inspectSlot('slot-12')` chính thức trả về `ok=true / repairable=false / messageCount=1 / issues=存档检查通过`, phân mảnh, số byte và checksum khớp từng mục với kết quả controller.
- [x] 2026-07-16: Chrome ngoài của P3 đã qua UI chính thức tạo save QA chủ tiệm tạm mới ở `slot-12` trống. Sau khi khởi tạo kết thúc, chẩn đoán tự động lưu là `dirty=false / saving=false`, kết quả gần nhất `status=saved / trigger=force`, lý do gộp chứa đồng thời "khởi tạo save mới hoàn thành" và "khởi tạo save mới hoàn thành". Đã ghi thực tế 1 phân mảnh, 9769 bytes, checksum `0706c24f`, mất 1102ms. Danh sách save chính thức chỉ có save QA đó, chứng minh trạng thái khởi tạo hậu kỳ sau save mới đã được lưu hoàn chỉnh lần thứ hai. Chẩn đoán đọc danh sách save vẫn dùng interface cũ hiển thị Loading, đã tắt rõ ràng sau khi lấy bằng chứng; trang game, hướng dẫn và save hoạt động vẫn giữ bình thường.
- [x] 2026-07-16: bundle cuối cùng của P3 đã inject lại vào Chrome ngoài. Lần đầu lỡ `import()` trực tiếp ở realm trang chính Tavern, do realm đó không có `waitGlobalInitialized` được Tavern Helper inject nên khởi tạo dừng lại; instance cũ đã unmount và không sinh save/AI/tầng. Sau đó đổi lại về realm script chính thức: qua iframe Tavern Helper `【Sanmingyue】预设工具` vẫn đang bật để load URL CORS cục bộ, HTTP 200, 8050420 bytes, SHA256 phía trình duyệt khớp với `38014966...02098` cục bộ. Cuối cùng chỉ có 1 iframe "Hoa Chưa Nở", màn hình tiêu đề hoàn chỉnh, Loading tắt; `gameUi` đã xuất hiện 3 phương thức chẩn đoán tự động lưu, snapshot ban đầu `dirty=false/saving=false`; "cho phép giám sát" đã tick. Danh sách save chính thức đọc vẫn rỗng, runtime `activeSlotId='' / activeMode=未选择`, sau khi xác nhận không có save thật của người dùng mới tạo QA.
- [x] 2026-07-16: cổng cục bộ cuối cùng trước máy thật của P3 đã pass. 8 file nguồn/test liên quan ESLint có định hướng 0 error/0 warning; `pnpm build:dev` toàn bộ mục tiêu webpack thành công, bundle Hoa Chưa Nở `dist/汤泉/脚本/零层美化/index.js` là 8050420 bytes, SHA256 `38014966BFE876137FC8700E91AC516B69EC7D1CA5C7BCCC696882ADE9902098`. Sau khi build đã chạy lại tự động lưu 7 nhóm và test rollback/đồng thời/8 phân mảnh world book, tất cả pass.
- [x] 2026-07-16: đã hoàn thành kiểm tra lại chênh lệch/race condition và sửa một khoảng thời gian lưu khi khởi tạo. Nguyên nhân là slot cố định sẽ ghi trạng thái ban đầu trước, sau đó mới thực hiện sắp xếp AI ngày đầu của khách, khách ngày đầu của nhân viên và khởi tạo MVU của từng chế độ; nếu đánh dấu clean ngay cho metadata `startSlot` sớm nhất, trạng thái hậu kỳ như lịch ngày đầu của khách có thể bị hiểu nhầm là đã lưu trong khi chưa ghi. Nay sau khi quy trình tạo mới kết thúc sẽ đánh dấu bẩn lại và phát một lần ép lưu im lặng hoàn chỉnh; nếu đồng bộ tầng nền vẫn còn xếp lịch thì giữ yêu cầu, retry sau. Đã đối chiếu thêm việc trước khi về tiêu đề, hiện trường nhân viên ghi MVU lại rồi mới lưu, đồng bộ nền vào cổng ngay từ khi "timer xếp lịch". 8 file nguồn/test P3 cuối cùng Prettier không đổi, ESLint 0 error/0 warning.
- [x] 2026-07-16: hồi quy chức năng cũ sau khi nối dây của P3 đã pass. 10 nhóm lịch sử hiện trường P0, phân tích phản hồi AI, phân tích nghiêm ngặt sắp xếp hàng ngày và áp lực 1000 vòng/queue đơn preset P1, đa giới tính nhân viên và migration ID save cũ, báo cáo bình thường/nghỉ bán P2/11 nhóm ngoại lệ từ chối và QA kinh doanh chuyên sâu đều pass; áp lực nhân viên 365 ngày/1440 lần phục vụ 0 lần fail khởi động và đạt hạng S, áp lực khách 3957 lần thao tác thành công, 492 ngày, 20 danh bạ và không thoát bất thường. Chứng tỏ nối dây tự động lưu không thay đổi dữ liệu xác định của 3 chế độ chơi, lịch sử P0, khách P1 hay hợp đồng báo cáo P2.
- [x] 2026-07-16: thêm `scripts/test_tangquan_worldbook_save.cjs` inject lỗi world book trong bộ nhớ và pass. Đã inject fail ở 3 giai đoạn: ghi phân mảnh staging, thay thế nguyên tử, ghi đĩa cứng, cả 3 lần đều khôi phục đúng entry `slot-01` gốc, đọc lại nội dung vẫn là `stable-old`, entry staging là 0, và log đều ghi lại một lần "lưu fail đã rollback, save cũ giữ nguyên". 3 test lưu nhiều phân mảnh phát cùng lúc đo được giao dịch ghi hoạt động tối đa là 1; chính văn chat/world book dài 495120 bytes sinh 8 phân mảnh và round-trip từng byte khớp nhau. Test tự động lưu và world book chạy gộp và pass, cả 2 file test ESLint 0 error/0 warning.
- [x] 2026-07-16: thêm `scripts/test_tangquan_autosave.cjs` hồi quy thời gian thuần túy và pass 7 nhóm assertion: gộp nhiều lý do và khoảng cách tối thiểu 90 giây, tính lại fallback rảnh 3 phút sau hoạt động người dùng, cổng bận và giữ retry 15 giây, yêu cầu ép mới trong lúc lưu chỉ nối thêm một lần lưu tuần tự, giữ trạng thái bẩn khi fail và tự động retry sau 60 giây, hủy timer khi clean thủ công và bỏ qua khi không có save hoạt động, cùng 1000 lần đánh dấu bẩn tần suất cao + 100 yêu cầu rời hiện trường cuối cùng chỉ ghi 1 lần. Test cũng chứng minh hàm lưu có đồng thời tối đa là 1; mã nguồn và test ESLint 0 error/0 warning.
- [x] 2026-07-16: nối dây App của P3 hoàn thành. Thay đổi trạng thái kinh doanh/khách/nhân viên thông thường sẽ đánh dấu bẩn; cả 3 quy trình qua ngày đều ép lưu im lặng sau khi báo cáo AI/lịch ngày hôm sau, MVU và entry động kết thúc; về tiêu đề và chuyển save sẽ chờ tối đa cửa sổ an toàn 5 giây rồi lưu, nếu fail hoặc vẫn bận thì hủy chuyển. AI hiệu lực trong hiện trường khi rời sẽ yêu cầu gộp lưu, tương tác danh bạ sau khi khôi phục hiện trường sẽ yêu cầu lưu, reroll/rollback/sửa xóa tầng bên ngoài sẽ giữ trạng thái bẩn. Cổng tự động lưu bao phủ `busy`, `airpSubmitting`, queue AI đơn, đồng bộ nền tầng/MVU/world book đang chạy và đã lên lịch, Loading và popup xác nhận. Lưu thủ công, load, tạo mới, lưu ngay khách trong ngày P1 và xóa save hoạt động đều xóa đúng cờ bẩn cũ; cổng chẩn đoán đã expose thành `tangquanFrontend.gameUi.inspectAutoSave()/markAutoSaveDirty()/requestAutoSave()`. 6 file liên quan ESLint có định hướng 0 error, `pnpm build:dev` vòng đầu sau nối dây toàn bộ mục tiêu webpack thành công, bundle Hoa Chưa Nở là 7.68 MiB.
- [x] 2026-07-16: việc ghi save world book của P3 đã đổi thành giao dịch an toàn khi fail. Save mới được ghi hoàn chỉnh trước bằng entry staging ẩn và xác minh, save cũ luôn được giữ nguyên trong lúc này; sau khi staging hoàn chỉnh mới thay thế một lần thành phân mảnh chính thức và ép ghi đĩa, đối chiếu lại index/số phân mảnh/chính văn/checksum; bất kỳ giai đoạn nào fail đều khôi phục world book hoàn chỉnh trước khi lưu. Lưu, lưu im lặng, import, sửa và xóa đều vào cùng một queue ghi FIFO, đảm bảo tại một thời điểm chỉ có một task ghi world book; log mới có số thứ tự hàng đợi, thời gian chờ/tổng, số phân mảnh, số byte, giá trị kiểm tra, giai đoạn fail và kết quả rollback. `worldbookSave.ts` và `autoSaveController.ts` ESLint có định hướng 0 error.
- [x] 2026-07-16: controller lưu hoàn chỉnh của P3 mới thêm `saveActiveDataQuiet()`, đường im lặng không hiển thị Loading, nhưng vẫn đọc và xác minh save cũ, bắt snapshot chat hoàn chỉnh, gộp dữ liệu frontend và gọi ghi an toàn world book. Lưu thường, tự động lưu im lặng, lưu-thành và tạo save mới nay dùng chung queue lưu FIFO tầng trên, phạm vi loại trừ lẫn nhau bao phủ từ đọc snapshot chat đến kết thúc ghi world book, tránh 2 lần lưu hoàn chỉnh bắt/ghi đè dữ liệu ở các khoảnh khắc khác nhau; log hàng đợi ghi số thứ tự, thời gian chờ, tổng thời gian và số lượng đang chờ xử lý. `saveController.ts` và `worldbookSave.ts` ESLint có định hướng 0 error.
- [x] 2026-07-16: lõi controller tự động lưu TypeScript thuần đã thêm mới tại `save/autoSaveController.ts`, không phụ thuộc Vue UI. Hiện đã hỗ trợ tập hợp phiên bản bẩn và lý do, khoảng cách tối thiểu 90 giây, fallback rảnh người dùng 3 phút, yêu cầu khoảng cách khi rời hiện trường, yêu cầu ép, gộp lần 2 khi có thay đổi mới trong lúc lưu, retry bận 15 giây, retry fail 60 giây, đối chiếu clean sau lưu thủ công, giải phóng timer và snapshot chẩn đoán hoàn chỉnh; ESLint có định hướng 0 error. Lõi này chưa nối vào App hay ghi save thực tế, TODO tương ứng của P3 tạm chưa tick.
- [x] 2026-07-16: audit hiện trạng của P3 hoàn thành. Chuỗi lưu hoàn chỉnh là `makeCurrentSaveDataPatch → saveActiveData/saveCurrentAsSlot → captureCurrentChatSnapshot → worldbookSave.writeSlot → SillyTavern.saveWorldInfo`, mỗi lần đều đọc toàn bộ chat và ghi lại phân mảnh save; code hiện có không có cờ bẩn thống nhất, gộp yêu cầu hay loại trừ lẫn nhau khi lưu. Đã định vị cổng ép: quyết toán/nghỉ ngơi của chủ tiệm, khách đi ngủ, nhân viên tan ca, về tiêu đề, bắt đầu hoặc load save khác; cổng rời hiện trường sau khi AI hiệu lực là `submitAirpInput/sendCustomerContactMessage → closeAirpScene`. Tín hiệu cấm lưu có thể đọc thống nhất `busy`, `airpSubmitting`, `aiGenerationPending`, Loading `visible`, `confirmState/pendingConfirmResolve`, và cần bổ sung đếm đồng bộ nền tầng/MVU. Ngoài ra xác nhận `worldbookSave.writeSlot()` hiện xóa phân mảnh cũ trước rồi mới tạo từng đợt phân mảnh mới, nếu fail giữa chừng sẽ để lại khoảng trống thiếu save; P3 bắt buộc phải đổi thành nội dung mới hoàn chỉnh sẵn sàng rồi thay thế một lần và giữ rollback nội dung cũ, không chỉ thêm timer ở tầng Vue.
- [x] 2026-07-16: P3 khởi động chính thức. Đã đọc lại hoàn chỉnh `AGENTS.md` gốc, 8 quy tắc `.cursor/rules` mà nó tham chiếu và danh sách bảo trì này, xác nhận tiếp tục tuân thủ: không sửa HTML preview, không phá vỡ 3 chế độ chơi/save/MVU/world book động/hướng dẫn, tất cả AI giữ queue đơn, không push GitHub/CDN. Hiện đang vào audit cổng lưu hoàn chỉnh và trạng thái bận, chưa thay đổi logic vận hành.
- [x] 2026-07-16: dọn dẹp QA cuối cùng của P2 hoàn thành. Qua UI chính thức về tiêu đề và xóa save chủ tiệm tạm slot-12 duy nhất; cuối cùng `slots=[] / activeSlotId='' / activeMode=未选择`. Sau khi inject lại trang chỉ có 1 iframe "Hoa Chưa Nở" và dừng ở màn hình tiêu đề hoàn chỉnh; 5 entry động (khu vực hiện tại/dự án/nhân viên/khách/ứng viên hiện tại) đều duy nhất, `enabled=false`, chính văn dài 0; MVU chỉ còn thời gian hiện tại, người dùng và `互动现场={}` rỗng, không tồn tại block chủ tiệm `未开之花汤泉`; preset `format=none`, tư duy cơ bản bật, output/tư duy chủ tiệm tắt, "cho phép giám sát" giữ bật. Listener cục bộ 8879 là 0, không có file truy vết yêu cầu/phản hồi mạng tạm nào, không xóa bất kỳ save thật nào của người dùng. P2 hoàn thành chính thức.
- [x] 2026-07-16: load save thật báo cáo AI của P2 đã pass. Đã thực hiện `loadSlot('slot-12')` chính thức, giá trị trả về khi load, save trước khi load và lần đọc lại sau khi load hoàn toàn khớp nhau: ngày kinh doanh 2, `2026年07月17日 星期五`, 00:00, vốn 126544, thu 11666, chi 13522, lãi -1856, `经营纪要.来源=ai / 营业日=1 / 问题=[]`, 4 đơn hàng dự án, nhân viên `atri/值班` và sự kiện `maintenance` đều không đổi. Số phân mảnh 1, 12671 bytes, checksum `8755fd8a` giữ nguyên; kiểm tra save vẫn là `ok=true / repairable=false`; preset giữ `none`.
- [x] 2026-07-16: đường thành công AI thật của báo cáo kinh doanh chủ tiệm P2 đã pass. Trước khi quyết toán, mạng baseline không có yêu cầu sinh nào của vòng này; sau khi xác nhận, định dạng chuyên dụng tạm chuyển thành `bossDaily`, tư duy cơ bản tắt, tư duy chủ tiệm bật, chỉ thêm mới 1 yêu cầu `/api/backends/chat-completions/generate` (reqid 1998, HTTP 200, thời gian server 15725.266 ms), hoàn thành thì preset khôi phục `none`. Trong yêu cầu, block dữ liệu `<boss_report_input>` thực tế chỉ xuất hiện 1 lần, `readonly=true`, ngày kinh doanh 1, khách 16, 6 khu vực, 4 dự án, 1 nhân viên và sự kiện `maintenance` đều đến từ sự thật đã đóng băng ở frontend; thu 11666, chi 13522, lãi -1856. Phản hồi gốc chỉ chứa duy nhất 1 `<boss_daily_report>` không thuộc tính, 10 trường cấp gốc, 4 dự án và đơn hàng 3/3/3/2, nhân viên `atri`, sự kiện `maintenance` đều khớp chính xác, `<content>/<UpdateVariable>/<hinking>/<ontent>` bắt được 0, chữ số Ả Rập và ký hiệu tiền tệ trong trường trần thuật bắt được 0, `finish_reason=stop`. Sau khi phân tích nghiêm ngặt ghi `经营纪要.来源=ai / 问题=[]` mới qua ngày kinh doanh 2, `2026年07月17日 星期五` 00:00; vốn từ 128400 chuyển chính xác thành 126544, save cập nhật thành 1 phân mảnh, 12671 bytes, kiểm tra pass.
- [x] 2026-07-16: sau khi xác nhận baseline save trống, đã qua UI chính thức tại slot-12 tạo QA chủ tiệm tạm `P2日报AI-QA` và bỏ qua hướng dẫn. Vốn ban đầu ¥128,400, runtime `activeSlotId=slot-12 / activeMode=老板`; save đầu tiên đã ghi 1 phân mảnh (9586 bytes), preset vẫn là `format=none`, output/tư duy chủ tiệm tắt. Chưa bấm quyết toán hay kích hoạt AI.
- [x] 2026-07-16: frontend nghiệm thu AI thật đã inject lại. Qua đường ES Module CORS cục bộ để load bundle hiện tại SHA256 `F0FC80AE33B59A6AE5CEF75B6A66C6EDD0E61424767BE8E31A0687FC9F4F4954`; sau khi gỡ khẩn cấp instance cũ chỉ giữ lại 1 iframe "Hoa Chưa Nở". Sau khi inject `slots=[]`, `activeSlotId` rỗng, `activeMode=未选择`, xác nhận không có save thật nào của người dùng; `bossDailyAvailable=true`, output/tư duy báo cáo chủ tiệm mỗi loại 1, mặc định output `none`, tư duy cơ bản bật, 2 entry chuyên dụng chủ tiệm tắt, chưa kích hoạt AI.
- [x] 2026-07-16: sau khi người dùng upload preset mới nhất, đã hoàn thành đối chiếu chỉ-đọc trạng thái chạy của Tavern. Preset đang load hiện tại là `明月秋青汤泉适配版`, "cho phép giám sát" của Tavern Helper đã bật; `💭 未开之花·经营日报思维` và `🎯 未开之花·老板经营日报输出` đều chỉ có 1, `role=system`, mặc định tắt. Độ dài chính văn runtime lần lượt là 1374 / 2487 ký tự, SHA256 lần lượt là `D9CAA852EA596A50811C83A39E0EFB0708B78446C7922E27DD5A2CABDB296502` / `F2155C287E49055ECDB2B71FB3F38C96F4438B0A09DB25C2EC71935D807921C6`, khớp từng byte với file nguồn cục bộ. Inject frontend và yêu cầu AI thật chưa bắt đầu.
- [x] 2026-07-16: sau khi sửa preset chuyên dụng của P2, build cục bộ hoàn chỉnh đã pass, 19 mục tiêu webpack đều thành công. Trong file nguồn và `明月秋青Maya.json` đã đóng gói, số lần bắt `<hinking>`, `<ontent>`, `boss_report_output_contract`, `boss_report_thinking` đều là 0; 2 tên runtime chính xác và 2 khai báo chế độ mới mỗi loại xuất hiện 1 lần trong JSON đã đóng gói. SHA256 JSON preset `0FB4CAAB5462FF622B5703961F69AD43BD8D6AC26C927A2FBC5F79F62199F3F2`, bundle frontend vẫn là `F0FC80AE33B59A6AE5CEF75B6A66C6EDD0E61424767BE8E31A0687FC9F4F4954`. Theo yêu cầu người dùng, chưa upload lên Tavern, chưa inject lại hay kích hoạt AI.
- [x] 2026-07-16: theo ủy quyền rõ ràng của người dùng, Codex hoàn thành sửa lỗi cơ học tối thiểu cho preset chuyên dụng P2, không sửa schema báo cáo ngày hay ràng buộc kinh tế chỉ-đọc. Tư duy báo cáo kinh doanh ngày đã gỡ vỏ bọc `boss_report_thinking` và theo khai báo task sắp xếp hàng ngày có sẵn tạm dừng quy tắc sáng tác không liên quan; output báo cáo ngày của chủ tiệm gỡ vỏ bọc `boss_report_output_contract`, đổi thành đặt `<thinking>` trước rồi output trực tiếp duy nhất `<boss_daily_report>`, cấm chính xác `<content>/<UpdateVariable>`. Trong file nguồn, số lần bắt `<hinking>`, `<ontent>` và 2 loại vỏ bọc đều là 0; XML mẫu có thể phân tích, 10 trường cấp gốc và trường con project/employee/event khớp từng mục với hợp đồng frontend. Chưa upload Tavern hay nghiệm thu AI thực tế.
- [x] 2026-07-16: lần đầu preset chuyên dụng P2 của Claude gửi lại đã hoàn thành nghiệm thu tĩnh, kết luận là chưa pass, chưa build, chưa inject, chưa kích hoạt AI thực tế. YAML 2 tên chính xác mỗi loại 1, mặc định tắt, vai trò và đường dẫn đều đúng, trường dữ liệu gốc và project/employee/event khớp với frontend; nhưng file nguồn và JSON đã đóng gói của output chủ tiệm vẫn chứa tag lỗi `<hinking>`, `<ontent>`, kết thúc vừa yêu cầu "duy nhất `boss_daily_report`" vừa yêu cầu "output sau content" tự mâu thuẫn, và không theo cấu trúc sắp xếp hàng ngày có sẵn để tạm dừng rõ ràng quy tắc văn phong/nhân vật/NSFW/chống giành lời. Không thể chỉ đơn giản thay `<ontent>` bằng `<content>`; mục tiêu đúng là đặt `<thinking>` trước rồi output trực tiếp duy nhất `<boss_daily_report>`, và cấm rõ ràng `<content>`. Vỏ bọc `boss_report_output_contract`/`boss_report_thinking` không phải yêu cầu của frontend, còn tăng rủi ro model copy thêm gốc thừa.
- [x] 2026-07-16: đường fallback cục bộ Chrome ngoài mục thứ 3 của P2 đã pass. Sau khi xác nhận preset đang chạy `bossDailyAvailable=false`, output/tư duy báo cáo ngày chủ tiệm mỗi loại 0, đã tạo QA chủ tiệm tạm ở slot-12 vốn không có save thật. Sau khi bấm quyết toán hôm nay, không sinh yêu cầu `/api/backends/chat-completions/generate` mới; frontend trong quy trình task duy nhất sinh bản tóm tắt kinh doanh cục bộ `来源=fallback`, rồi đẩy ngày kinh doanh 1 / `2026年07月16日 星期四` thành ngày kinh doanh 2 / `2026年07月17日 星期五` 00:00 và lưu ngay. Thu ngày đầu 11666, chi 13522, lãi -1856, vốn chuyển chính xác từ 128400 thành 126544; UI hiển thị bản tóm tắt cục bộ hoàn chỉnh.
- [x] 2026-07-16: save và load fallback của P2 đã pass. `loadSlot('slot-12')` thật cho thấy ngày kinh doanh, ngày, giờ, vốn, thu, chi, lãi, `经营纪要.来源=fallback`, ngày kinh doanh tóm tắt 1 và vấn đề `老板经营日报输出格式不可用` đều giữ nguyên.
- [x] 2026-07-16: dọn dẹp QA của P2 hoàn thành. Qua quy trình về tiêu đề và xóa save chính thức đã gỡ slot-12; cuối cùng slots=[], activeSlotId rỗng, activeMode=`未选择`, preset format=`none`, 5 entry động khu vực/dự án/nhân viên hiện tại/khách hiện tại/ứng viên hiện tại đều disabled và chính văn dài 0, MVU không tồn tại block kinh doanh, hiện trường tương tác rỗng, sau khi inject lại trang khôi phục màn hình tiêu đề và Loading tắt, service cục bộ 8879 đã dừng. Không xóa bất kỳ save thật nào của người dùng.
- [x] 2026-07-16: hồi quy mã hoàn chỉnh mục thứ 2 của P2 đã pass. Test báo cáo ngày chủ tiệm bao phủ ngày kinh doanh bình thường, ngày nghỉ bán, migration save cũ, fallback cục bộ và 11 nhóm từ chối nghiêm ngặt; QA kinh doanh chuyên sâu chủ tiệm bao phủ ma trận kinh doanh 12 kịch bản 90 ngày, áp lực cực đoan 160 ngày, nạp/gỡ MVU và save JSON round-trip; test sắp xếp hàng ngày/preset bao phủ phân tích 1000 vòng và chuyển đổi tư duy độc quyền `bossDaily` cho queue đơn. 7 file nguồn/test P2 ESLint 0 error, `pnpm build:dev` 19 mục tiêu webpack đều thành công. Bundle mới nhất SHA256 `F0FC80AE33B59A6AE5CEF75B6A66C6EDD0E61424767BE8E31A0687FC9F4F4954`; JSON preset trước khi Claude sửa vẫn là hash P1 `A31508244083CC5C097CA1505663B09E0A9374B83E80344D76AB4EF3A8736B9D`.
- [x] 2026-07-16: parser báo cáo ngày chủ tiệm của P2 đã hoàn thành gia cố thêm. Nay sẽ từ chối thuộc tính trên tag gốc hoặc bất kỳ tag con nào, trường text lồng tag con, và một `boss_daily_report` thứ hai giả dạng có thuộc tính; thêm 4 nhóm hồi quy XML độc hại/bất thường. `test_tangquan_boss_daily_report.cjs` các case ngày kinh doanh hợp lệ, ngày nghỉ bán và cả 11 nhóm từ chối đều pass, ESLint có định hướng 0 error.
- [x] 2026-07-16: Chrome ngoài sau khi trang bị crash refresh đã inject lại bản build dev P2 hiện tại. Đầu tiên gọi gỡ khẩn cấp chính thức của instance chạy cũ để dọn UI, listener và controller, rồi qua iframe script "【Sanmingyue】预设工具" vẫn đang bật để load `dist/汤泉/脚本/零层美化/index.js`; yêu cầu mạng 200, SHA256 bundle là `B54B6164A522AA69F79AEE2899F4F637F3653BA0B907E521D7BC57365CCEA2DF`. Cuối cùng chỉ có 1 iframe "Hoa Chưa Nở", màn hình tiêu đề hoàn chỉnh, Loading tắt, "cho phép giám sát" của Tavern Helper vẫn tick, chưa tạo save, chưa kích hoạt AI hay quyết toán.
- [x] 2026-07-16: hoàn thành audit hiện trạng P2. Cổng qua ngày chính thức của chủ tiệm là `App.vue::closeBossDay()` và `restBossToday()`; hiện tại gọi trực tiếp `settleBossDay()`/`restBossOneDay()`, hàm thuần tính một lần thu, chi, lương, thay đổi nhân viên, đánh giá dự án, suy giảm bảo trì, tiến độ thi công và đẩy ngày/ngày kinh doanh sang ngày hôm sau ngay lập tức. Quy trình hiện có không có đóng băng sự thật, tóm tắt kinh doanh, queue AI, fallback fail hay mốc lưu sau quyết toán.
- [x] 2026-07-16: xác định thiết kế tương thích tối thiểu của P2: không tách sửa công thức kinh tế đã ổn định; gọi hàm thuần có sẵn trên bản clone trạng thái cũ trước để lấy trạng thái ứng viên cuối cùng bất biến và sự thật chỉ-đọc, nhưng chưa commit vào trạng thái Vue trước khi báo cáo hoàn thành. Output AI chỉ được phép tham chiếu tên/đơn hàng dự án, tên/vai trò nhân viên và ID sự kiện mà frontend đưa ra, không ghi lại bất kỳ số tiền nào. Khi phân tích nghiêm ngặt fail hoặc preset chuyên dụng không khả dụng thì sinh tóm tắt cục bộ. Sau khi ghi tóm tắt vào `结算.经营纪要` mới commit trạng thái qua ngày một lần và lưu. Nghỉ một ngày và quyết toán bình thường dùng chung một quy trình, save cũ thiếu tóm tắt sẽ migration thành null.
- [x] 2026-07-16: sửa lỗi cuối cùng của Claude cho P1 đã được nghiệm thu tĩnh theo thực tế repo. Output nhân viên, tư duy chung và các cấm `<time>` liên quan đến nhân viên trong `makeWaiterDailyFormatInstruction()` đều đã gỡ; mỗi khách mới chỉ điền một gender tự nhiên, tổng thể khách mới không cố định đều thỏa mãn đa dạng. Nguồn hồ sơ khách ứng viên/AI khách mới của frontend nhân viên đã phân luồng rõ ràng. Các tồn đọng `<active_nomination>` cũ, `<guest>` cũ trong assignment, cấm tạo khách mới đều bắt được 0; hàm khách đúng theo thỏa thuận chưa sửa dòng thứ 117.
- [x] 2026-07-16: hồi quy độc lập sau khi nộp hợp đồng P1 cuối cùng đều pass: ESLint có định hướng 9 file 0 error; phân tích nghiêm ngặt/áp lực 1000 vòng/queue đơn preset pass; fallback đa giới tính, migration xác định save cũ, MVU/world book cùng guestId pass; 10 nhóm lịch sử hiện trường pass; 365 ngày/1440 lần phục vụ 0 lần fail khởi động và đạt hạng S.
- [x] 2026-07-16: đường thành công AI Chrome ngoài của P1 đã pass. Tư duy hàng ngày runtime/output nhân viên khớp từng chữ với preset cuối cùng cục bộ, "cho phép giám sát" bật; ngày đầu của `slot-12` chỉ vào queue duy nhất sequence 1, sau 59.951 giây pending=0. Phản hồi gốc mạng chứa `<waiter_daily>` v2 hoàn chỉnh, trường gốc, 3 hồ sơ guest và 3 assignment `guest_id` đầy đủ; frontend chỉ ghi `guestGeneration.source=ai / issues=[]` sau khi phân tích nghiêm ngặt pass, và lưu ngay `g_0716_1/g_0716_2/g_0716_3`.
- [x] 2026-07-16: đa dạng và ổn định khách AI của P1 đã pass máy thật. Lai Ân=nam/người, Ailica=nữ/bán tiên, Mor=nam/thú nhân, thỏa mãn tối thiểu 2 giới tính; gọi lặp lại cùng ngày bị cổng attempted bỏ qua trực tiếp và không sinh sequence 2; đọc save và `loadSlot(slot-12)` thật trước/sau đều source và 3 ID hoàn toàn giống nhau.
- [x] 2026-07-16: toàn chuỗi guestId AI của P1 đã pass máy thật. `g_0716_1` đầu tiên hoàn toàn khớp trong guest registry, assignment, speakerId/participantIds hiện trường, scene.客人ID, MVU `当前接客._客人ID`, ID trong world book động khách hiện tại; dự án, giới tính, chủng tộc, nơi xuất thân, ngân sách và sở thích đồng bộ khớp nhau.
- [x] 2026-07-16: dọn dẹp QA của P1 hoàn thành. Đầu tiên load snapshot slot-12 trước khi mở việc để hủy hiện trường test, rồi qua quy trình xóa chính thức xóa save QA duy nhất; cuối cùng slots=[], lastMessageId=0, activeMode=`未选择`, activeSlotId rỗng, airpState=null, block biến nhân viên rỗng, 5 entry động khu vực/dự án/nhân viên/khách/ứng viên hiện tại đều disabled và chính văn dài 0, output preset khôi phục none, trang về tiêu đề, service 8879 dừng.

- [x] 2026-07-16: sau khi nhận báo cáo "hoàn thành" lần 2 của Claude, đã đối chiếu lại thực tế repo, xác nhận nội dung trả về vẫn là bản viết lại v2 lần đầu chứ không phải sửa tối thiểu như yêu cầu: hợp đồng output dòng 25/70, tư duy sắp xếp hàng ngày dòng 19/34/37, `makeWaiterDailyFormatInstruction()` dòng 229/235 đều được giữ nguyên; 3 chỗ cấm `<time>`, việc viết lẫn gender đơn giá trị/tập hợp và mâu thuẫn "tất cả giá trị đến từ input" đều chưa sửa cái nào. Thời gian ghi cuối cùng của 3 file vẫn là 2026-07-15 23:46–23:47, nên chưa rebuild, inject hay tiêu tốn yêu cầu AI thật lần nào, cổng P1 vẫn chưa pass.

- [x] 2026-07-16: sau khi nhận báo cáo "Claude đã hoàn thành" lần 2, đã đọc lại 3 file nguồn; kết quả hoàn toàn giống lần nộp đầu tiên, bản sửa tối thiểu được yêu cầu vòng trước thực tế chưa ghi xuống: dòng 70 output nhân viên, dòng 37 tư duy sắp xếp hàng ngày, dòng 235 `makeWaiterDailyFormatInstruction()` vẫn cấm `<time>` bắt buộc; gender vẫn viết lẫn giá trị đơn với đa dạng tổng thể; dòng 34 tư duy vẫn yêu cầu mọi giá trị đến từ input. Vì nghiệm thu tĩnh fail và trạng thái chạy Chrome ngoài vẫn là schema cũ, vòng này chưa phát yêu cầu AI vô nghĩa, chưa tạo save QA, chưa vào P2.
- [x] 2026-07-15: kiểm tra chỉ-đọc Chrome ngoài hoàn thành: trang hiện tại vẫn là `http://127.0.0.1:8000/`, "cho phép giám sát" của Tavern Helper đã tick, tên preset đang bật là "明月秋青汤泉适配版". Độ dài `💭 每日安排思维` runtime là 623, output nhân viên là 1565, vẫn là hợp đồng cũ (chứa `<active_nomination>` và `<guest>` assignment, không chứa `<guests>/<guest_id>` bản mới); do đó dù file nguồn build thành công cũng không thể trực tiếp làm đường thành công AI, sau khi sửa cuối cùng còn phải đồng bộ 3 nội dung runtime với phiên bản file nguồn.
- [x] 2026-07-15: hồi quy mã P1 sau khi Claude sửa đã chạy lại: ESLint có định hướng 9 file liên quan 0 error; phân tích nghiêm ngặt nhân viên/khách, áp lực 1000 vòng và assertion chuyển đổi queue preset đều pass; đa giới tính khách, migration xác định save cũ, MVU/world book cùng ID pass; 10 nhóm lịch sử hiện trường pass; áp lực 365 ngày/1440 lần phục vụ 0 lần fail khởi động và đạt hạng S. 2 script TypeScript lần đầu gọi trực tiếp `ts-node` fail do cấu hình ESM/Node types của repo, sau khi đổi sang cách đăng ký transpile-only CommonJS giống test `.cjs` có sẵn thì assertion đều pass, mã nguồn không bị ảnh hưởng.
- [x] 2026-07-15: đã đối chiếu tĩnh 3 chỗ sửa P1 của Claude. Trường cấp gốc, guest và assignment của `waiter_daily` đều khớp với parser v2 frontend, `<active_nomination>` cũ, `<guest>` cũ trong assignment và ràng buộc "chỉ được dùng khách trong input/cấm tạo khách" đều bắt được 0; đồng thời phát hiện dòng 70 hợp đồng output, dòng 37 tư duy sắp xếp hàng ngày và inject frontend nhân viên vẫn cấm `<time>`, mâu thuẫn trực tiếp với `<time>` bắt buộc cấp gốc; ngoài ra phát hiện việc viết lẫn gender đơn với đa dạng giới tính toàn thể, cuối tư duy "tất cả giá trị từ input" mâu thuẫn với việc sinh khách mới, đã ngăn việc phán đoán nhầm P1 pass trước khi sửa.
- [x] 2026-07-15: nghiệm thu toàn chuỗi guestId Chrome ngoài của P1 đã pass. Sau khi rebuild/inject lại cuối cùng, cùng một `guest-fallback-d1-male-3` hoàn toàn giống nhau ở 7 chỗ: guest registry, assignment, speakerId hiện trường, participantIds, scene.客人ID, MVU `_客人ID` và world book động `ID`; entry khách hiện tại khi bật chứa đầy đủ hồ sơ giới tính/chủng tộc/nguồn gốc/ngân sách/sở thích/chỉ định.
- [x] 2026-07-15: nghiệm thu UI tối thiểu menu nhân viên của P1 đã pass, không tái cấu trúc layout; danh sách tiếp khách hiển thị thật "nam · thú nhân · ngân sách ¥4,200", "nữ · tiên nữ · ngân sách ¥4,800", "song tính · ma tộc · ngân sách ¥6,200", 3 lịch xếp ca và thời gian dự án bình thường.
- [x] 2026-07-15: dọn dẹp QA của P1 hoàn thành. Load slot-12 lưu snapshot rồi hủy trạng thái ca/hiện trường trước khi xóa save tạm đó; cuối cùng danh sách save rỗng, lastMessageId=0, runtime=`未选择`, airpState=null, cả 5 entry động đều disabled và chính văn dài 0, service tĩnh CORS cục bộ 8879 đã dừng.
- [x] 2026-07-15: đường fallback preset cũ/schema lỗi Chrome ngoài của P1 đã pass. Ngày đầu nhân viên slot-12 chỉ vào queue duy nhất sequence 1; output `active_nomination + assignment.guest` cũ bị parser nghiêm ngặt từ chối, mở đầu vẫn vào play bình thường, tự động giữ đủ 3 khách fallback nam/nữ/song tính hoàn chỉnh. Sequence 1 sau 20129ms pending=0, sau đó version 2, guestIds, hồ sơ hoàn chỉnh và lý do phân tích fail được ghi ngay vào slot-12 (0.01 MB).
- [x] 2026-07-15: chống trùng cùng ngày và ổn định khi load save của P1 đã pass. Yêu cầu lại cùng ngày ghi ngay "đã sinh, bỏ qua yêu cầu trùng", không vào queue AI; load lại slot-12 thì 3 guestId/xưng hô/giới tính và guestGeneration hoàn toàn không đổi, không sinh task mới.
- [x] 2026-07-15: hoàn thành triển khai tầng dữ liệu của P1. `WaiterPageState` nâng lên version 2, thêm guest registry hoàn chỉnh, dailyGuestIds, guestGeneration. assignment/nomination thêm guestId. Save version 1 cũ migration theo hash xác định từ xưng hô thành `guest-legacy-*`, chuẩn hóa lặp lại vẫn cùng ID, load không random lại.
- [x] 2026-07-15: fallback cục bộ của P1 đổi thành hồ sơ khách hoàn chỉnh và sinh xác định theo nhiều nhóm nam/nữ/vô tính hoặc phi nhị nguyên, không còn dùng danh sách cố định "cô/quý bà"; mỗi khách gồm xưng hô, giới tính, chủng tộc, nơi xuất thân, ngân sách, sở thích dự án, khách quen/dấu chỉ định, số ngày chỉ định còn lại và ghi chú đặc biệt.
- [x] 2026-07-15: parser nghiêm ngặt của P1 hoàn thành. `<waiter_daily>` bản mới bắt buộc chứa tham chiếu guestId 1-1 giữa guests và assignments, xác minh ID duy nhất, trường bắt buộc, khoảng ngân sách, dự án/khu vực/thời lượng hợp lệ, không chồng lấp trong ca, nguồn và dấu nhất quán, tối thiểu 2 giới tính khách mới, danh tính/dự án/số ngày chỉ định xuyên ngày không được sửa; phản hồi rỗng, gốc trùng, trường thừa, ID lạ, ngân sách vượt giới hạn và giới tính đơn lẻ đều bị từ chối và giữ fallback.
- [x] 2026-07-15: ID của P1 đã xuyên suốt lịch xếp ca, chỉ định xuyên ngày, speakerId/participantIds hiện trường, MVU `_客人ID` và world book động `ID`; MVU/world book đồng thời ghi hồ sơ khách hoàn chỉnh. Menu nhân viên trong layout ổn định gốc đã bổ sung hiển thị giới tính, chủng tộc và ngân sách.
- [x] 2026-07-15: sinh hàng ngày của P1 tiếp tục tái sử dụng `enqueueAiGeneration()` duy nhất; sau attempted cùng ngày bỏ qua yêu cầu trùng. AI thành công hay bất kỳ nhánh fail nào đều lưu trạng thái ngày hôm đó; save fail chỉ ghi lỗi và giữ trạng thái memory. Save nhân viên mới đã persist trạng thái fallback hoàn chỉnh trước request API đầu tiên.
- [x] 2026-07-15: xác minh dữ liệu thuần vòng đầu của P1 đã pass: áp lực 365 ngày/1440 lần phục vụ, 4 chỉ định xuyên ngày tương thích, 1000 vòng phân tích nghiêm ngặt, AI thay thế ID fallback, từ chối rỗng/sai định dạng, MVU/world book cùng ID, migration xác định save cũ đều pass; ESLint có định hướng 0 error, `pnpm build:dev` toàn bộ mục tiêu webpack pass. 10 nhóm lịch sử hiện trường P0 vẫn pass.
- [x] 2026-07-15: hoàn thành audit luồng dữ liệu hiện trạng P1. Xác nhận nguyên nhân gốc: `waiterGame.ts` trong `makeAssignments()` sinh `guest: string` từ 8 xưng hô nữ cố định; API hàng ngày nhân viên hiện có chỉ xác minh và ghi lại opening/shiftNote của các assignment đã định sẵn này, cấm rõ ràng tạo khách, nên không thể thay đổi giới tính hay danh tính. Assignment, chỉ định xuyên ngày, MVU, world book và hiện trường hiện tại đều chỉ truyền xưng hô, không có guestId ổn định hay hồ sơ khách hoàn chỉnh.
- [x] 2026-07-15: xác định thiết kế tương thích P1: trạng thái nhân viên nâng lên phiên bản 2, thêm guest registry, guestId cho mỗi assignment/nomination và metadata sinh hàng ngày; save cũ chỉ có xưng hô dùng "chuẩn hóa xưng hô + hash xác định" để migration ID ổn định, không random lại. Mỗi ngày xây dựng fallback xác định chứa nhiều giới tính khác nhau trước, rồi để queue AI duy nhất dùng XML nghiêm ngặt thay thế toàn bộ khách mới trong ngày; hồ sơ chỉ định xuyên ngày tái sử dụng nguyên trạng theo guestId.
- [x] 2026-07-15: dọn dẹp QA phúc tra P0 hoàn thành. Đầu tiên load snapshot gốc slot-12 để khôi phục chat từ tầng 10 về tầng 0 và hủy MVU danh bạ tạm, rồi xóa slot-12; cuối cùng danh sách save rỗng, runtime activeMode=`未选择`, activeSlotId rỗng, airpState=null, trạng thái liên hệ atri khôi phục "chưa thêm", danh bạ rỗng. `area.current`, `project.current`, 5 entry động nhân viên/khách/ứng viên hiện tại đều disabled và chính văn dài 0.
- [x] 2026-07-15: P0 chốt phiên bản chính thức đã pass. Sau khi Claude sửa, vòng đầu, A/B/C liên tục, thường/reroll, danh bạ A/B đều chỉ đọc đúng `<interactive_input>` thật. Kết hợp với bằng chứng chuyển đổi nhân vật, hiện trường 3 người, chuyển từ 3 người sang 1 người, rollback/xóa tầng/sửa/load save đã pass trước đó trên cùng bundle frontend chưa sửa, toàn bộ hạng mục nghiệm thu P0 đã khép kín, cho phép vào P1.
- [x] 2026-07-15: phúc tra ngữ nghĩa và cách ly vòng 2 danh bạ B của Claude sau khi sửa đã pass. Lịch sử trước đó của danh bạ B chỉ có 2 mục A/"Tinh Sa" của cùng `contact-character-atri`, không trộn lẫn A/B/C offline; lịch sử "Vân Mẫu" bắt được 0, append 1, model phản hồi online "Vân Mẫu". Queue duy nhất sequence 7 sau 82136ms pending=0.
- [x] 2026-07-15: phúc tra ngữ nghĩa vòng đầu danh bạ của Claude sau khi sửa đã pass. Chỉ trong bản sao MVU của swipe hiện tại tầng trợ lý QA tạm 6 đặt trạng thái liên hệ atri thành "đã thêm" và kích hoạt cổng mở khóa `MESSAGE_UPDATED` chính thức; danh bạ A có sceneId=`contact-character-atri`, lịch sử trước đó rỗng, input hiện tại bắt được 0, append 1, model phản hồi online là "Tinh Sa". Queue duy nhất sequence 6 hoàn thành thì pending=0.
